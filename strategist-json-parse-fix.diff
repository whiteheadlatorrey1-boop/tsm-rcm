--- html/healthcare/hc-main-strategist.html	2026-08-15 02:28:58.521418366 +0000
+++ /tmp/hc-main-strategist-test.html	2026-08-15 04:55:59.653146501 +0000
@@ -1015,7 +1015,16 @@
       // Render explainability panel
       if(jsonMatch){
         try{
-          const jsonStr = jsonMatch[1].trim().replace(/```json|```/g,'').trim();
+          let jsonStr = jsonMatch[1].trim().replace(/```json|```/g,'').trim();
+          // The model sometimes prefixes the JSON block with markdown (e.g.
+          // "**JSON Data:**") instead of pure JSON. Extract just the object
+          // between the first { and last } so stray formatting doesn't break
+          // the parse.
+          const firstBrace = jsonStr.indexOf('{');
+          const lastBrace = jsonStr.lastIndexOf('}');
+          if (firstBrace !== -1 && lastBrace > firstBrace) {
+            jsonStr = jsonStr.slice(firstBrace, lastBrace + 1);
+          }
           const obj = JSON.parse(jsonStr);
           renderExplainPanel(obj);
           try { if (window.TSMWorkflowStage) TSMWorkflowStage.write('escalate-exec', { confidence: obj.confidence }); } catch(e) {}
