diff --git a/html/bpo-files/bpo-internal1.html b/html/bpo-files/bpo-internal1.html
index 35885045..91da748b 100644
--- a/html/bpo-files/bpo-internal1.html
+++ b/html/bpo-files/bpo-internal1.html
@@ -378,9 +378,12 @@ html,body{height:100%;font-family:-apple-system,BlinkMacSystemFont,'Segoe UI',sa
 
   <div class="nav-divider"></div>
   <div class="sidebar-section">Tools</div>
-  <a class="nav-item" href="/html/war-rooms/bpo-war/bpo-executive-portal.html">
+  <a class="nav-item" href="/html/war-rooms/bpo-war/bpo-executive-portal.html?vertical=bpo">
     <i class="ti ti-terminal-2"></i> Command Center
   </a>
+  <a class="nav-item" href="/html/enterprise/enterprise-executive-portal.html?vertical=bpo">
+    <i class="ti ti-building-skyscraper"></i> Enterprise Portal
+  </a>
   <a class="nav-item" href="/tsm-doc-search-multi.html">
     <i class="ti ti-users"></i> Client Portal
   </a>
diff --git a/html/enterprise/enterprise-executive-portal.html b/html/enterprise/enterprise-executive-portal.html
index eed811f6..29dda1b4 100644
--- a/html/enterprise/enterprise-executive-portal.html
+++ b/html/enterprise/enterprise-executive-portal.html
@@ -63,6 +63,8 @@ body{background:var(--bg);color:var(--text);font-family:'Rajdhani',sans-serif;mi
 .cap-domain{font-family:'JetBrains Mono',monospace;font-size:.58rem;color:var(--cyan);margin-top:3px;}
 .cap-score{font-family:'Orbitron',sans-serif;font-size:1rem;font-weight:700;flex-shrink:0;}
 .cap-finding{font-size:.7rem;color:var(--muted);margin-top:8px;line-height:1.4;}
+.cap-link{display:inline-block;font-size:.7rem;color:var(--cyan);margin-top:8px;text-decoration:none;border-bottom:1px solid rgba(6,182,212,.35);}
+.cap-link:hover{border-bottom-color:var(--cyan);}
 .cap-none{grid-column:1/-1;text-align:center;color:var(--muted);font-family:'JetBrains Mono',monospace;font-size:.7rem;padding:20px;}
 
 .ai-box{background:rgba(0,212,212,.06);border:1px solid var(--cyan);border-radius:6px;padding:22px;margin-bottom:16px;white-space:pre-wrap;font-size:.95rem;line-height:1.55;}
@@ -142,6 +144,17 @@ function renderVsel(){
 }
 renderVsel();
 
+// Deep-link support: ?vertical=healthcare auto-runs that vertical's demo
+// on load, so BPO Internal1 / HC Denial can link straight into their own
+// vertical here instead of landing on the generic selector.
+(function(){
+  try {
+    const params = new URLSearchParams(window.location.search);
+    const v = params.get('vertical');
+    if (v && VERTICALS.some(x => x.key === v)) runDecision(v);
+  } catch (e) {}
+})();
+
 function scoreColor(score){
   if(score>=80) return 'red';
   if(score>=50) return 'amber';
@@ -172,17 +185,26 @@ function renderResult(data){
   const content = document.getElementById('content');
   const caps = data.capabilities || [];
   const relevant = caps.length;
+  const totalCaps = data.totalCapabilities || 10;
   const highScore = caps.reduce((m,c)=>Math.max(m, c.score||0), 0);
   const bnca = data.bnca || {};
 
   const kpiRow = `
     <div class="kpi-row">
       <div class="kpi"><div class="kpi-val cyan">${(data.sector||'—').toUpperCase()}</div><div class="kpi-lbl">VERTICAL</div></div>
-      <div class="kpi"><div class="kpi-val">${relevant}/10</div><div class="kpi-lbl">CAPABILITIES RELEVANT</div></div>
+      <div class="kpi"><div class="kpi-val">${relevant}/${totalCaps}</div><div class="kpi-lbl">CAPABILITIES RELEVANT</div></div>
       <div class="kpi"><div class="kpi-val ${scoreColor(highScore)}">${highScore}%</div><div class="kpi-lbl">HIGHEST SIGNAL SCORE</div></div>
       <div class="kpi"><div class="kpi-val ${bnca.priority==='HIGH'?'red':'green'}">${bnca.confidence!=null?bnca.confidence+'%':'—'}</div><div class="kpi-lbl">BNCA CONFIDENCE</div></div>
     </div>`;
 
+  // case-engine is the one capability backed by a real, browsable queue
+  // (TSMCaseWidget on the BPO exec portal) rather than a self-contained
+  // score — so its card gets a real link into that queue, filtered to
+  // this vertical, instead of just a finding string.
+  const caseQueueLink = c => c.id === 'case-engine'
+    ? `<a class="cap-link" href="/html/war-rooms/bpo-war/bpo-executive-portal.html?vertical=${encodeURIComponent(data.sector||'')}" target="_blank" rel="noopener">View live case queue &rarr;</a>`
+    : '';
+
   const capGrid = caps.length
     ? caps.map(c => `
         <div class="cap-cell">
@@ -194,6 +216,7 @@ function renderResult(data){
             <div class="cap-score" style="color:var(--${scoreColor(c.score||0)})">${c.score}%</div>
           </div>
           <div class="cap-finding">${(c.findings&&c.findings[0])||'No specific finding surfaced.'}</div>
+          ${caseQueueLink(c)}
         </div>`).join('')
     : '<div class="cap-none">No capabilities were relevant to this document context.</div>';
 
@@ -212,7 +235,7 @@ function renderResult(data){
     </div>` : '';
 
   content.innerHTML = `
-    <div class="status-line">${(data.sector||'').toUpperCase()} &middot; ${data.documentType||''} &mdash; ${relevant} of 10 capabilities relevant.</div>
+    <div class="status-line">${(data.sector||'').toUpperCase()} &middot; ${data.documentType||''} &mdash; ${relevant} of ${totalCaps} capabilities relevant.</div>
     ${kpiRow}
     <div class="sec">
       <div class="sec-hdr">CAPABILITY SIGNAL GRID</div>
diff --git a/html/healthcare/hc-denial-war-room.html b/html/healthcare/hc-denial-war-room.html
index 11671336..7aff7e88 100644
--- a/html/healthcare/hc-denial-war-room.html
+++ b/html/healthcare/hc-denial-war-room.html
@@ -367,6 +367,9 @@ textarea::placeholder{color:var(--faint)}
 /* ── DEFECT MANIFEST ─────────────────────────────────────── */
 .manifest-btn{position:fixed;bottom:18px;left:18px;z-index:7000;background:#0d1117;border:1px solid #ef4444;color:#ef4444;font-family:'Orbitron',sans-serif;font-size:.62rem;padding:7px 14px;border-radius:4px;cursor:pointer;letter-spacing:.08em;opacity:.7}
 .manifest-btn:hover{opacity:1}
+.pipeline-links{position:fixed;bottom:18px;right:18px;z-index:7000;display:flex;gap:8px}
+.pipeline-links a{background:#0d1117;border:1px solid var(--cyan3);color:var(--cyan);font-family:'Orbitron',sans-serif;font-size:.62rem;padding:7px 14px;border-radius:4px;text-decoration:none;letter-spacing:.06em;opacity:.75;transition:opacity .15s}
+.pipeline-links a:hover{opacity:1}
 .manifest-overlay{position:fixed;inset:0;background:rgba(0,0,0,.82);z-index:9500;display:none;align-items:center;justify-content:center}
 .manifest-overlay.active{display:flex}
 .manifest-card{background:#0d1117;border:1px solid #ef4444;border-radius:8px;padding:28px 32px;max-width:600px;width:92%;max-height:78vh;overflow-y:auto}
@@ -385,6 +388,13 @@ textarea::placeholder{color:var(--faint)}
 <script src="https://code.responsivevoice.org/responsivevoice.js?key=FREE"></script>
 <script src="/html/healthcare/js/tsm-workflow-stage.js"></script>
 <script src="/html/shared/tsm-output-contract.js"></script>
+<!-- Case Engine — see syncStructuredCaseToEngine() below. Was previously
+     not loaded on this page at all: the 5-engine pipeline built a full
+     13-field structured case object (buildHCStructuredCase) and then just
+     held it in a page-local variable, so no denial worked here ever
+     reached the same case-queue/executive-rollup pipeline BPO Internal1
+     and the BPO exec portal use. -->
+<script src="/html/shared/tsm-case-manager.js"></script>
 <script src="/js/tsm-runtime-lock.js"></script>
 <script src="/js/tsm-event-contract.js"></script>
 <script src="/js/tsm-autonomy-layer.js"></script>
@@ -1803,6 +1813,7 @@ async function runPipeline() {
   // pulled verbatim from engine text, or a documented deterministic
   // derivation of a value the engines already stated (see comments).
   lastStructuredCase = buildHCStructuredCase(outputs, docText, claimDollar);
+  syncStructuredCaseToEngine(lastStructuredCase);
 
   setTimeout(() => {
     try {
@@ -2003,6 +2014,60 @@ function executePacketCompilation() {
 // in the same script scope without threading params through everything
 // in between).
 let lastStructuredCase = null;
+// caseId of the Case Engine record mirroring lastStructuredCase, once
+// syncStructuredCaseToEngine() has run at least once this session. Kept so
+// a re-run of the pipeline (retry, re-upload) updates the same case
+// instead of creating a duplicate every time.
+let syncedCaseId = null;
+
+/**
+ * syncStructuredCaseToEngine(sc) -> TSMCase | null
+ *
+ * Mirrors the 13-field structured case contract (buildHCStructuredCase)
+ * into the Case Engine (TSMCaseManager), the same server-synced system
+ * (bpo_cases, /api/bpo/reports/*) BPO Internal1 intakes now reach via
+ * mission-case-bridge.js and the BPO exec portal writes to directly. This
+ * is what makes a denial actually worked in this war room show up in the
+ * live case queue and the Enterprise Portal's case-engine capability —
+ * before this, lastStructuredCase never left the page.
+ *
+ * No tenant/session context exists on this standalone tool page, so
+ * tenantId is left null (case-manager's documented default — same as
+ * omitting tenantId anywhere else in this codebase: unscoped, not broken).
+ */
+function syncStructuredCaseToEngine(sc) {
+  if (!sc || typeof TSMCaseManager === 'undefined') return null;
+  const severity = (!sc.appealable || sc.recoveryLikelihood === 'WEAK') ? 'high'
+    : (sc.recoveryLikelihood === 'MODERATE' ? 'med' : 'low');
+  const patch = {
+    sector: 'healthcare',
+    vertical: 'healthcare',
+    client: sc.payer || '',
+    process: 'denial_recovery',
+    source: 'hc_denial_war_room',
+    title: (sc.claimId ? sc.claimId + ' — ' : '') + (sc.denialReasonCode || 'Denial'),
+    description: sc.rootCauseHypothesis || '',
+    priority: TSMCaseManager.priorityFor(severity, sc.confidence),
+    exposure: typeof sc.financialExposure === 'number' ? sc.financialExposure : null,
+    deadline: sc.appealDeadline || null,
+    confidence: typeof sc.confidence === 'number' ? sc.confidence : null,
+    confidenceTier: sc.confidenceTier || null,
+    humanReviewRequired: sc.humanReviewRequired,
+    recommendedActions: sc.recoveryLikelihood ? ['Recovery likelihood: ' + sc.recoveryLikelihood] : [],
+    fields: { claimId: sc.claimId, denialCategory: sc.denialCategory, appealable: sc.appealable }
+  };
+  try {
+    if (syncedCaseId && TSMCaseManager.getById(syncedCaseId)) {
+      return TSMCaseManager.update(syncedCaseId, patch);
+    }
+    const rec = TSMCaseManager.create(patch);
+    syncedCaseId = rec.caseId;
+    return rec;
+  } catch (e) {
+    console.error('[case-engine] sync failed', e);
+    return null;
+  }
+}
 
 /**
  * buildHCStructuredCase(outputs, docText, claimDollar)
@@ -2374,6 +2439,13 @@ function exportAll() {
 
 <!-- ── DEFECT MANIFEST ───────────────────────────────────────────── -->
 <button class="manifest-btn" id="manifestBtn" style="display:none" onclick="document.getElementById('manifestOverlay').classList.add('active')">⚠ DEFECT MANIFEST</button>
+<!-- Cases created in this war room sync to the Case Engine (tsm-case-manager.js,
+     server-persisted in bpo_cases) — these jump to the same live views BPO
+     Internal1 and the Enterprise Portal use, filtered to this vertical. -->
+<div class="pipeline-links">
+  <a href="/html/war-rooms/bpo-war/bpo-executive-portal.html?vertical=healthcare" target="_blank" rel="noopener">CASE QUEUE →</a>
+  <a href="/html/enterprise/enterprise-executive-portal.html?vertical=healthcare" target="_blank" rel="noopener">ENTERPRISE PORTAL →</a>
+</div>
 <script>
 // Internal-only: defect manifest is a dev/QA tool, never shown in a client-facing session
 // unless explicitly requested via ?debug=1 (or ?debug=true).
diff --git a/html/shared/tsm-case-manager.js b/html/shared/tsm-case-manager.js
index 031cb538..d6e4f46a 100644
--- a/html/shared/tsm-case-manager.js
+++ b/html/shared/tsm-case-manager.js
@@ -529,6 +529,12 @@
     summarize: summarize,
     confidenceTierFor: confidenceTierFor,
     humanReviewRequiredFor: humanReviewRequiredFor,
+    // Was internal-only. hc-denial-war-room.html's syncStructuredCaseToEngine()
+    // needs the same severity+confidence -> P1/P2/P3 mapping create() already
+    // uses internally, rather than re-implementing it — exporting it is
+    // strictly additive (create()/createFromException() still call the
+    // local closure directly, unaffected).
+    priorityFor: priorityFor,
     syncToServer: syncToServer,
     hydrateFromServer: hydrateFromServer
   };
diff --git a/html/war-rooms/bpo-war/bpo-executive-portal.html b/html/war-rooms/bpo-war/bpo-executive-portal.html
index 479bcc9d..4dfd8840 100644
--- a/html/war-rooms/bpo-war/bpo-executive-portal.html
+++ b/html/war-rooms/bpo-war/bpo-executive-portal.html
@@ -739,12 +739,12 @@ body::before{content:'';position:fixed;inset:0;background:repeating-linear-gradi
     </div>
 
     <div class="panel">
-      <div class="panel-title"><div class="dot" style="background:var(--red)"></div>EXCEPTION QUEUE · SECTOR: BPO</div>
+      <div class="panel-title"><div class="dot" style="background:var(--red)"></div>EXCEPTION QUEUE · SECTOR: <span id="exceptionQueueSector">BPO</span></div>
       <div id="tsm-exception-queue"></div>
     </div>
 
     <div class="panel">
-      <div class="panel-title"><div class="dot" style="background:var(--cyan)"></div>CASE QUEUE · SECTOR: BPO</div>
+      <div class="panel-title"><div class="dot" style="background:var(--cyan)"></div>CASE QUEUE · SECTOR: <span id="caseQueueSector">BPO</span></div>
       <div id="tsm-case-queue"></div>
     </div>
 
@@ -1809,11 +1809,25 @@ window.TSM_AUTONOMY_STANDARDIZED = true;
 <script src="/html/js/widgets/tsm-case-widget.js"></script>
 <script>
 (function(){
+  // This portal is the only page in the repo with a real, wired case-queue
+  // widget (TSMCaseWidget), so it doubles as the universal case browser for
+  // the HC Denial / BPO Internal1 / Enterprise Portal cross-links below —
+  // ?vertical=healthcare (etc.) switches the queue instead of hardcoding
+  // 'bpo'. Omitting the param keeps the exact prior behavior (sector: bpo).
+  const _params = new URLSearchParams(window.location.search);
+  const _vertical = _params.get('vertical') || 'bpo';
+  const _tenantId = _params.get('tenantId') || null;
+  const _sectorLabel = _vertical.toUpperCase();
+  const _exceptionSectorEl = document.getElementById('exceptionQueueSector');
+  const _caseSectorEl = document.getElementById('caseQueueSector');
+  if (_exceptionSectorEl) _exceptionSectorEl.textContent = _sectorLabel;
+  if (_caseSectorEl) _caseSectorEl.textContent = _sectorLabel;
+
   if (typeof TSMExceptionWidget !== 'undefined') {
-    TSMExceptionWidget.mount('tsm-exception-queue', { sector: 'bpo' });
+    TSMExceptionWidget.mount('tsm-exception-queue', { sector: _vertical });
   }
   if (typeof TSMCaseWidget !== 'undefined') {
-    TSMCaseWidget.mount('tsm-case-queue', { sector: 'bpo' });
+    TSMCaseWidget.mount('tsm-case-queue', { sector: _vertical, tenantId: _tenantId });
   }
 
   // Wire Exception Queue "Resolve" clicks into the same exec-action relay
diff --git a/server/enterprise/api/enterprise-router.js b/server/enterprise/api/enterprise-router.js
index 903b6a15..8e6f8a27 100644
--- a/server/enterprise/api/enterprise-router.js
+++ b/server/enterprise/api/enterprise-router.js
@@ -112,6 +112,9 @@ function reshapeForClient(result, context) {
             capabilityCount:
                 capabilities.length,
 
+            totalCapabilities:
+                result.enrichment.totalCapabilities || capabilities.length,
+
             capabilities,
 
             verticalFinding:
diff --git a/server/enterprise/enterprise-engine.js b/server/enterprise/enterprise-engine.js
index f9256396..130ecb56 100644
--- a/server/enterprise/enterprise-engine.js
+++ b/server/enterprise/enterprise-engine.js
@@ -232,6 +232,13 @@ class EnterpriseEngine {
             capabilities:
                 results,
 
+            // Was hardcoded to 10 on the client ("X of 10 capabilities
+            // relevant") — broke the moment case-engine.js became the 11th
+            // module. Threading the real loaded-module count through here
+            // instead so the client never has to hardcode it again.
+            totalCapabilities:
+                capabilities.length,
+
 
             summary:{
 
