diff --git a/html/war-rooms/insure-war/insurance-executive-portal.html b/html/war-rooms/insure-war/insurance-executive-portal.html
index ceaee18c..006bcc9e 100644
--- a/html/war-rooms/insure-war/insurance-executive-portal.html
+++ b/html/war-rooms/insure-war/insurance-executive-portal.html
@@ -673,27 +673,6 @@ function populateFromRelay(d) {
     document.getElementById('relayAge').textContent = age < 1 ? 'Just synced' : `Synced ${age}m ago`;
   }
   if (n4) document.getElementById('relayContent').textContent = n4.slice(0, 420) + (n4.length > 420 ? '...' : '');
-    // ── TSM PATCH: wire stratJson KPIs into existing portal elements ──
-    (function wireInsuranceKPIs() {
-      try {
-        var raw = JSON.parse(localStorage.getItem('tsm_ins_strat_relay') || sessionStorage.getItem('TSM_INS_STRAT_RELAY') || '{}');
-        var sj = raw.stratJson || raw.json || raw;
-        var set = function(id, val) { var e = document.getElementById(id); if (e && val) e.textContent = val; };
-        set('kpiExposure',   sj.noActionRevLoss);
-        set('kpiRecovery',   sj.actionRevLoss);
-        set('kpiNetRisk',    sj.recoveryTime);
-        set('kpiConfidence', sj.confidence != null ? sj.confidence + '%' : null);
-        // Ticker dupes
-        set('tk1',  sj.noActionRevLoss); set('tk1b', sj.noActionRevLoss);
-        set('tk2',  sj.confidence != null ? sj.confidence + '%' : null);
-        set('tk2b', sj.confidence != null ? sj.confidence + '%' : null);
-        set('tk3',  sj.actionRevLoss);   set('tk3b', sj.actionRevLoss);
-        set('tk4',  sj.recoveryTime);    set('tk4b', sj.recoveryTime);
-        console.log('[TSM-INS-EXEC] KPIs wired from stratJson');
-      } catch(e) { console.warn('[TSM-INS-EXEC] KPI wire failed:', e); }
-    })();
-    // ── END TSM PATCH ────────────────────────────────────────────────
-
 
   // ── Parse Node 4 (Executive Package) ──
   const expM   = n4.match(/FINANCIAL IMPACT[:\s]+([^\n]+)/i);
@@ -714,10 +693,10 @@ function populateFromRelay(d) {
   const windowM = n1.match(/STRATEGIC WINDOW[:\s]+([^\n]+)/i);
   const topRisks = [...n1.matchAll(/^\d\.\s*([^—\n]+)—\s*(\$[^—\n]+)—\s*([^\n]+)/gm)].slice(0,3);
 
-  // ── Extract exposure figure (from Node 4, fallback strategist snapshot) ──
+  // ── Extract exposure figure (from Node 4 FINANCIAL IMPACT line, fallback strategist snapshot) ──
   let exposureVal = '—';
-  const expDirect = n4.match(/total exposure(?: is)?\s*\$?([\d,]+(?:\s*[–\-]\s*\$?[\d,]+)?)/i) || (d.warRoomSnapshot && d.warRoomSnapshot.exposure ? [null, d.warRoomSnapshot.exposure] : null);
-  if (expDirect) exposureVal = ('$'+expDirect[1].replace(/^\$/,'')).slice(0,22);
+  const impactAmt = expM ? expM[1].match(/\$[\d,]+(?:\s*[–\-]\s*\$?[\d,]+)?/) : null;
+  if (impactAmt) exposureVal = impactAmt[0].slice(0,22);
   else if (d.warRoomSnapshot && d.warRoomSnapshot.exposure) exposureVal = String(d.warRoomSnapshot.exposure).slice(0,22);
 
   const recoverableVal = recM ? ('$'+recM[1].replace(/^\$/,'')).slice(0,16) : '—';
diff --git a/html/war-rooms/insure-war/insurance-war-room.html b/html/war-rooms/insure-war/insurance-war-room.html
index e3529f59..4a9e105c 100644
--- a/html/war-rooms/insure-war/insurance-war-room.html
+++ b/html/war-rooms/insure-war/insurance-war-room.html
@@ -391,7 +391,6 @@ body::before{content:'';position:fixed;top:0;right:0;width:600px;height:600px;ba
   <button class="nav-btn" onclick="nav('ins-hub.html')">HUB</button>
   <button class="nav-btn" onclick="nav('../../tsm-insurance/pc-command.html')">P&C COMMAND</button>
   <button class="nav-btn" onclick="nav('../../tsm-insurance/dme.html')">DME</button>
-  <button class="nav-btn" onclick="nav('ins-anomaly-advisor.html')">ANOMALY ADVISOR</button>
   <div class="nav-right">
     <button class="tour-launch-btn" id="tourLaunchBtn" onclick="startTour()">▶ GUIDED TOUR</button>
     <span id="navClock" class="nav-clock">--:--:--</span>
@@ -616,7 +615,6 @@ body::before{content:'';position:fixed;top:0;right:0;width:600px;height:600px;ba
       </div>
       <div class="eb-btns" id="escalBtns">
         <button class="btn btn-gold" onclick="escalateToStrategist()">⚜ ESCALATE TO STRATEGIST →</button>
-        <button class="btn btn-outline" onclick="openAnomalyAdvisor()">◈ ANOMALY ADVISOR</button>
         <button class="btn btn-outline" onclick="exportFull()">EXPORT ANALYSIS</button>
         <button class="btn btn-ghost" onclick="clearAll()">CLEAR & RESET</button>
       </div>
@@ -625,13 +623,6 @@ body::before{content:'';position:fixed;top:0;right:0;width:600px;height:600px;ba
   </main>
 </div>
 
-<div id="anomalyAutoStrip" style="display:none;position:fixed;bottom:32px;right:24px;z-index:200;background:#0a1510;border:1px solid rgba(56,189,248,.4);border-radius:4px;padding:12px 16px;display:none;align-items:center;gap:14px;font-family:'Share Tech Mono',monospace;font-size:11px;color:#38bdf8;letter-spacing:.06em;box-shadow:0 4px 24px rgba(0,0,0,.5);">
-  <span style="color:#1ee8b6;">◈</span>
-  <span>6-ENGINE COMPLETE — RUN ANOMALY ADVISOR?</span>
-  <button onclick="openAnomalyAdvisor();document.getElementById('anomalyAutoStrip').style.display='none';" style="background:rgba(56,189,248,.12);border:1px solid #38bdf8;color:#38bdf8;font-family:'Share Tech Mono',monospace;font-size:10px;letter-spacing:.08em;padding:5px 12px;border-radius:3px;cursor:pointer;">OPEN →</button>
-  <button onclick="document.getElementById('anomalyAutoStrip').style.display='none';" style="background:transparent;border:none;color:#445544;font-size:14px;cursor:pointer;padding:0 4px;">✕</button>
-</div>
-
 <div class="status-bar">
   <div class="sb-dot"></div>
   <span>TSM · Insurance War Room</span>
@@ -640,7 +631,7 @@ body::before{content:'';position:fixed;top:0;right:0;width:600px;height:600px;ba
   <span class="sb-sep">·</span>
   <span id="sbDoc">No document loaded</span>
   <span class="sb-sep">·</span>
-  <span>Groq: openai/gpt-oss-120b</span>
+  <span>AI ENGINE: ACTIVE</span>
   <span class="sb-sep">·</span>
   <span>TOKENS: <span id="sbTokens" style="color:var(--gold)">0</span> / <span id="sbCost" style="color:var(--green)">$0.0000</span></span>
 </div>
@@ -721,12 +712,12 @@ function setDocType(btn,type){
 function saveKey(){ groqKey=document.getElementById('groqKey').value.trim(); localStorage.setItem('tsm_groq_key',groqKey); }
 async function testKey(){
   groqKey=document.getElementById('groqKey').value.trim();
-  if(!groqKey){alert('Enter Groq API key first.');return;}
+  if(!groqKey){alert('Enter your API key first.');return;}
   addLog('Testing API key...','','a');
   try{
     const r=await fetch('/api/groq/validate-key',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({apiKey:groqKey})});
     const d=await r.json();
-    if(d.ok) addLog('API key valid','Groq connection confirmed','g');
+    if(d.ok) addLog('API key valid','AI engine connection confirmed','g');
     else addLog('API key invalid','Check key and retry','f');
   }catch(e){addLog('Connection error',e.message,'f');}
 }
@@ -876,44 +867,10 @@ async function fireEngines(){
   document.getElementById('spinner').classList.remove('active');
   document.getElementById('fireBtn').disabled=false;
   addLog('All 6 Engines Complete','Ready to escalate to Insurance Strategist','p');
-  addLog('Anomaly Advisor','Auto-analysis available — click ANOMALY ADVISOR or open node','p');
 
   storeRelay(e);
   fireCapabilitySweep_INS(e);
 
-  // ── TSM Mission Guide trigger ──────────────────────────────────────────
-  if (window.TSMMissionGuide) {
-    const denialCodeMatch = ((e[2]||'')+(e[1]||'')).match(/\b(CO-\d+|PR-\d+|OA-\d+|PI-\d+)\b/i);
-    const exposureMatch   = (e[3]||'').match(/TOTAL EXPOSURE[:\s]+([^\n]+)/i);
-    const patientMatch    = (e[1]||'').match(/PATIENT[:\s]+([^\n]+)/i);
-    const payerMatch      = (e[1]||'').match(/PAYER[:\s]+([^\n]+)/i);
-    window.TSMMissionGuide.triggerFromEngine({
-      anomalyType:     selectedDocType + ' · Denial Analysis',
-      anomalySummary:  (e[2]||'').slice(0, 200),
-      denialCode:      denialCodeMatch ? denialCodeMatch[1].toUpperCase() : null,
-      claimAmount:     exposureMatch   ? exposureMatch[1].trim().slice(0, 20) : null,
-      patientOrClient: patientMatch    ? patientMatch[1].trim().slice(0, 40) : null,
-      payer:           payerMatch      ? payerMatch[1].trim().slice(0, 30)   : null,
-      targetApp:       'Insurance War Room'
-    });
-  }
-
-  // Auto-show anomaly advisor panel after 1.2s
-  setTimeout(() => {
-    try {
-      sessionStorage.setItem('tsmInsWarRoom', JSON.stringify({ docText, selectedDocType, timestamp: Date.now() }));
-    } catch(err) {}
-    if(typeof showAnomalyAdvisorPanel === 'function') {
-      showAnomalyAdvisorPanel([e[1]||'',e[2]||'',e[3]||'',e[4]||''], docText);
-    }
-  }, 1200);
-}
-
-function openAnomalyAdvisor() {
-  try {
-    sessionStorage.setItem('tsmInsWarRoom', JSON.stringify({ docText, selectedDocType, timestamp: Date.now() }));
-  } catch(e) {}
-  window.open('ins-anomaly-advisor.html', '_blank');
 }
 
 function launchApp(href, appMeta){
@@ -1224,12 +1181,12 @@ const TOUR_SLIDES = [
   },
   {
     id: 'groq',
-    title: 'STEP 3 · CONNECT GROQ API',
+    title: 'STEP 3 · CONNECT AI ENGINE',
     type: 'action',
     target: 'groqSection',
     autoMs: 6000,
-    narration: `The war room runs on <span class="hl">Groq's openai/gpt-oss-120b</span> — the fastest inference endpoint for this workload. Enter your <span class="cmd">gsk_...</span> key, hit TEST to verify the connection. Your key is stored locally in <span class="warn">localStorage</span> and never transmitted to TSM servers. If you don't have a key, the engines fall back to structured fallback outputs — useful for demos.`,
-    actionHint: 'Enter your Groq API key (gsk_...) and click TEST to validate'
+    narration: `The war room runs on the <span class="hl">TSM Neural Core</span> — a high-speed AI engine tuned for this workload. Enter your API key, hit TEST to verify the connection. Your key is stored locally in <span class="warn">localStorage</span> and never transmitted to TSM servers. If you don't have a key, the engines fall back to structured fallback outputs — useful for demos.`,
+    actionHint: 'Enter your API key and click TEST to validate'
   },
   {
     id: 'fire',
@@ -1237,7 +1194,7 @@ const TOUR_SLIDES = [
     type: 'action',
     target: 'fireBtn',
     autoMs: 7000,
-    narration: `Click <span class="hl">⚡ FIRE ALL 6 ENGINES</span> to launch the sequential analysis pipeline. The engines run <span class="cmd">in dependency order</span>: Engine 01 extracts facts → 02 flags risk using 01's output → 03 + 04 run in parallel using 01+02 → 05 synthesizes 03+04 → 06 routes to the right apps. Each engine takes ~2-4 seconds on Groq. <span class="warn">Do not navigate away</span> during the run — state is stored on completion.`,
+    narration: `Click <span class="hl">⚡ FIRE ALL 6 ENGINES</span> to launch the sequential analysis pipeline. The engines run <span class="cmd">in dependency order</span>: Engine 01 extracts facts → 02 flags risk using 01's output → 03 + 04 run in parallel using 01+02 → 05 synthesizes 03+04 → 06 routes to the right apps. Each engine takes ~2-4 seconds. <span class="warn">Do not navigate away</span> during the run — state is stored on completion.`,
     actionHint: 'Click ⚡ FIRE ALL 6 ENGINES to launch the analysis pipeline'
   },
   {
@@ -1246,7 +1203,7 @@ const TOUR_SLIDES = [
     type: 'demo',
     target: 'engineStatusPanel',
     autoMs: 6000,
-    narration: `Watch the <span class="hl">Engine Status panel</span> in real time. Each engine cycles: <span class="warn">IDLE → RUNNING → COMPLETE</span>. The amber pulsing badge means that engine is calling Groq. Green COMPLETE means output is stored and downstream engines have access to it. If an engine errors out, the system falls back to structured heuristic output so the chain doesn't break.`,
+    narration: `Watch the <span class="hl">Engine Status panel</span> in real time. Each engine cycles: <span class="warn">IDLE → RUNNING → COMPLETE</span>. The amber pulsing badge means that engine is running. Green COMPLETE means output is stored and downstream engines have access to it. If an engine errors out, the system falls back to structured heuristic output so the chain doesn't break.`,
     actionHint: null
   },
   {
@@ -1574,7 +1531,7 @@ endTour = function(){ speechStop(); _etOrig(); };
     <div class="howto-section">
       <h3>STEP 2 — CONFIGURE API KEY</h3>
       <ul>
-        <li>Enter your Groq API key and hit Test. Green dot = connected. Saved to localStorage.</li>
+        <li>Enter your API key and hit Test. Green dot = connected. Saved to localStorage.</li>
       </ul>
     </div>
     <div class="howto-section">
@@ -1615,6 +1572,7 @@ endTour = function(){ speechStop(); _etOrig(); };
         <tr><td>D-07</td><td>Mobile Layout</td><td>Engine output panels overflow on screens &lt;768px</td><td class="sev-low">LOW</td><td>OPEN</td></tr>
       </tbody>
     </table>
+    <div style="margin-top:10px;font-size:.75rem;color:#666;">See D-04 in context: <a href="#" onclick="document.getElementById('insManifestOverlay').classList.remove('active');window.insTourOpen();return false;" style="color:var(--cyan);text-decoration:underline;">launch the Guided Tour →</a></div>
     <button class="manifest-close-btn" onclick="document.getElementById('insManifestOverlay').classList.remove('active')">CLOSE</button>
   </div>
 </div>
@@ -1671,7 +1629,7 @@ const INS_TOUR_STEPS = [
   { title: 'E4 · RECOVERY STRATEGY', body: 'Builds a step-by-step appeal and re-submission plan with regulatory escalation path, documentation checklist, and carrier contact protocol.' },
   { title: 'E5 · NAVIGATOR', body: 'Routes each action to the correct role — Claims Adjuster, Underwriter, Compliance Officer, or Carrier Contact — with priority and escalation order.' },
   { title: 'NARRATION & DISPATCH', body: 'Use the 🎙 SPEAK button for ResponsiveVoice narration of engine output. The App Dispatch panel recommends the best Insurance suite app for your document type.' },
-  { title: 'YOU\'RE READY', body: 'Load a claims document or hit a sample, enter your Groq key, and run the pipeline. Hit HOW TO anytime for a full control walkthrough.' },
+  { title: 'YOU\'RE READY', body: 'Load a claims document or hit a sample, enter your API key, and run the pipeline. Hit HOW TO anytime for a full control walkthrough.' },
 ];
 
 let insTourIdx = 0;
@@ -1716,728 +1674,6 @@ window.insHowtoOpen = () => document.getElementById('insHowtoOverlay').classList
 <script src="/html/shared/js/tsm-assistant-widget.js"></script>
 <script src="/html/core/tsm-runtime.js"></script>
 
-<script src="/js/tsm-mission-guide.js"></script>
-
-<style>
-/* ── Mission Preview Modal ─────────────────────────────────── */
-#tmg-mission-modal-overlay {
-  display: none;
-  position: fixed;
-  inset: 0;
-  background: rgba(0,0,0,0.85);
-  z-index: 99998;
-  align-items: center;
-  justify-content: center;
-  backdrop-filter: blur(2px);
-}
-#tmg-mission-modal-overlay.active { display: flex; }
-
-#tmg-mission-modal {
-  width: 540px;
-  max-height: 88vh;
-  background: #0a0e0a;
-  border: 1px solid #2a4a2a;
-  border-top: 3px solid #39ff14;
-  border-radius: 4px;
-  font-family: 'JetBrains Mono', 'Share Tech Mono', monospace;
-  font-size: 11px;
-  color: #c8e6c9;
-  display: flex;
-  flex-direction: column;
-  overflow: hidden;
-  box-shadow: 0 0 60px rgba(57,255,20,0.15);
-  animation: tmg-modal-in 0.2s ease;
-}
-@keyframes tmg-modal-in {
-  from { transform: translateY(12px); opacity: 0; }
-  to   { transform: translateY(0);    opacity: 1; }
-}
-
-.tmg-modal-header {
-  padding: 14px 16px;
-  background: #0d140d;
-  border-bottom: 1px solid #1a3a1a;
-  display: flex;
-  justify-content: space-between;
-  align-items: flex-start;
-}
-.tmg-modal-header-title {
-  color: #39ff14;
-  font-size: 11px;
-  font-weight: 700;
-  letter-spacing: 0.12em;
-  text-transform: uppercase;
-  margin-bottom: 3px;
-}
-.tmg-modal-header-sub {
-  color: #5a8a5a;
-  font-size: 9px;
-  letter-spacing: 0.08em;
-}
-.tmg-modal-close {
-  background: none;
-  border: 1px solid #2a4a2a;
-  color: #5a8a5a;
-  width: 22px; height: 22px;
-  border-radius: 2px;
-  font-size: 11px;
-  cursor: pointer;
-  display: flex; align-items: center; justify-content: center;
-  flex-shrink: 0;
-  margin-left: 12px;
-}
-.tmg-modal-close:hover { border-color: #ff4444; color: #ff4444; }
-
-.tmg-modal-body { overflow-y: auto; flex: 1; padding: 14px 16px; }
-
-.tmg-modal-anomaly {
-  background: #0f1f0f;
-  border: 1px solid #1e3d1e;
-  border-left: 3px solid #ffd600;
-  padding: 10px 12px;
-  border-radius: 2px;
-  margin-bottom: 12px;
-}
-.tmg-modal-anomaly-label { color: #ffd600; font-size: 9px; letter-spacing: 0.1em; text-transform: uppercase; margin-bottom: 4px; }
-.tmg-modal-anomaly-type { color: #fff; font-size: 12px; font-weight: 700; margin-bottom: 5px; }
-.tmg-modal-anomaly-details { color: #8ab48a; font-size: 10px; line-height: 1.5; }
-
-.tmg-modal-app-row {
-  display: flex;
-  align-items: center;
-  gap: 10px;
-  padding: 10px 12px;
-  background: #0d1a0d;
-  border: 1px solid #1a3a1a;
-  border-radius: 3px;
-  margin-bottom: 12px;
-}
-.tmg-modal-app-star { color: #ffd600; font-size: 14px; }
-.tmg-modal-app-info { flex: 1; }
-.tmg-modal-app-rank { color: #5a8a5a; font-size: 9px; letter-spacing: 0.1em; text-transform: uppercase; }
-.tmg-modal-app-name { color: #39ff14; font-size: 12px; font-weight: 700; }
-
-.tmg-modal-steps-preview {
-  margin-bottom: 12px;
-}
-.tmg-modal-steps-label {
-  color: #5a8a5a;
-  font-size: 9px;
-  letter-spacing: 0.1em;
-  text-transform: uppercase;
-  margin-bottom: 8px;
-}
-.tmg-modal-step-row {
-  display: flex;
-  gap: 8px;
-  margin-bottom: 8px;
-  padding: 8px 10px;
-  background: #0d140d;
-  border: 1px solid #1a3a1a;
-  border-radius: 2px;
-}
-.tmg-modal-step-num {
-  color: #39ff14;
-  font-size: 9px;
-  font-weight: 700;
-  min-width: 16px;
-}
-.tmg-modal-step-content {}
-.tmg-modal-step-title { color: #c8e6c9; font-size: 10px; font-weight: 700; margin-bottom: 2px; }
-.tmg-modal-step-instruction { color: #7aaa7a; font-size: 10px; line-height: 1.4; margin-bottom: 4px; }
-.tmg-modal-step-hint {
-  background: #071207;
-  border-left: 2px solid #ffd600;
-  padding: 4px 7px;
-  color: #ffd600;
-  font-size: 9.5px;
-  line-height: 1.4;
-}
-
-.tmg-modal-generating {
-  text-align: center;
-  padding: 28px 0;
-}
-.tmg-modal-gen-spinner {
-  width: 28px; height: 28px;
-  border: 2px solid #1a3a1a;
-  border-top-color: #39ff14;
-  border-radius: 50%;
-  animation: tmg-spin 0.8s linear infinite;
-  margin: 0 auto 12px;
-}
-@keyframes tmg-spin { to { transform: rotate(360deg); } }
-.tmg-modal-gen-text { color: #5a8a5a; font-size: 10px; line-height: 1.7; letter-spacing: 0.06em; }
-.tmg-modal-gen-label { color: #39ff14; font-size: 9px; letter-spacing: 0.12em; text-transform: uppercase; margin-bottom: 6px; }
-
-.tmg-modal-footer {
-  border-top: 1px solid #1a3a1a;
-  padding: 12px 16px;
-  display: flex;
-  gap: 8px;
-  justify-content: flex-end;
-  flex-shrink: 0;
-}
-.tmg-modal-btn {
-  padding: 8px 16px;
-  border-radius: 2px;
-  font-family: inherit;
-  font-size: 10px;
-  font-weight: 700;
-  letter-spacing: 0.1em;
-  text-transform: uppercase;
-  cursor: pointer;
-  transition: all 0.15s;
-}
-.tmg-modal-btn-primary {
-  background: #39ff14;
-  color: #000;
-  border: none;
-}
-.tmg-modal-btn-primary:hover { opacity: 0.88; transform: translateY(-1px); }
-.tmg-modal-btn-primary:disabled { opacity: 0.4; cursor: not-allowed; transform: none; }
-.tmg-modal-btn-secondary {
-  background: none;
-  color: #5a8a5a;
-  border: 1px solid #2a4a2a;
-}
-.tmg-modal-btn-secondary:hover { border-color: #39ff14; color: #39ff14; }
-
-/* ── Upgrade existing recommendation cards ─── */
-.tmg-card-launch-btn {
-  display: block;
-  width: 100%;
-  margin-top: 10px;
-  padding: 7px 0;
-  background: transparent;
-  border: 1px solid #2a5a1a;
-  color: #39ff14;
-  font-family: 'JetBrains Mono', monospace;
-  font-size: 9px;
-  font-weight: 700;
-  letter-spacing: 0.12em;
-  text-transform: uppercase;
-  cursor: pointer;
-  border-radius: 2px;
-  transition: all 0.15s;
-  text-align: center;
-}
-.tmg-card-launch-btn:hover {
-  background: #39ff14;
-  color: #000;
-  border-color: #39ff14;
-}
-</style>
-
-<!-- Mission Preview Modal -->
-<div id="tmg-mission-modal-overlay">
-  <div id="tmg-mission-modal">
-    <div class="tmg-modal-header">
-      <div>
-        <div class="tmg-modal-header-title">⚡ Mission Briefing</div>
-        <div class="tmg-modal-header-sub" id="tmg-modal-subtitle">Generating guided mission plan...</div>
-      </div>
-      <button class="tmg-modal-close" id="tmg-modal-close-btn">✕</button>
-    </div>
-    <div class="tmg-modal-body" id="tmg-modal-body">
-      <div class="tmg-modal-generating">
-        <div class="tmg-modal-gen-spinner"></div>
-        <div class="tmg-modal-gen-label">TSM Neural Core</div>
-        <div class="tmg-modal-gen-text">Analyzing anomaly...<br>Generating step-by-step mission plan...</div>
-      </div>
-    </div>
-    <div class="tmg-modal-footer" id="tmg-modal-footer" style="display:none">
-      <button class="tmg-modal-btn tmg-modal-btn-secondary" id="tmg-modal-cancel-btn">Cancel</button>
-      <button class="tmg-modal-btn tmg-modal-btn-primary" id="tmg-modal-launch-btn" disabled>
-        Launch with Guide →
-      </button>
-    </div>
-  </div>
-</div>
-
-<script>
-(function () {
-  'use strict';
-
-  const STORAGE_KEY = 'tsm_active_mission';
-  let _pendingMission = null;
-
-  // ── Inject "Launch with Guide" button into each recommendation card ────────
-  function upgradeRecommendationCards() {
-    // Targets the existing OPEN ... → buttons and their parent cards
-    const openBtns = document.querySelectorAll('[class*="open-btn"], .rec-open-btn, a[href*=".html"]');
-
-    // More robust: find all card-level containers by looking for the rank labels
-    // Adjust selector to match your actual War Room card structure
-    const cards = document.querySelectorAll('.recommendation-card, .rec-card, [data-app-rank]');
-
-    if (cards.length > 0) {
-      cards.forEach(card => upgradeCard(card));
-    } else {
-      // Fallback: find by the open buttons pattern
-      document.querySelectorAll('button, a').forEach(btn => {
-        const text = (btn.textContent || '').toUpperCase();
-        if (text.includes('OPEN ') && text.includes('→')) {
-          injectLaunchBtn(btn.closest('[class]') || btn.parentElement, btn);
-        }
-      });
-    }
-  }
-
-  function upgradeCard(card) {
-    if (card.dataset.tmgUpgraded) return;
-    card.dataset.tmgUpgraded = '1';
-
-    const appName = extractAppName(card);
-    const appUrl  = extractAppUrl(card);
-    const rank    = card.dataset.appRank || card.querySelector('[class*="rank"]')?.textContent || '';
-
-    const btn = document.createElement('button');
-    btn.className = 'tmg-card-launch-btn';
-    btn.textContent = '⚡ Launch with Mission Guide';
-    btn.addEventListener('click', (e) => {
-      e.preventDefault();
-      e.stopPropagation();
-      openMissionModal(appName, appUrl, rank, card);
-    });
-    card.appendChild(btn);
-  }
-
-  function injectLaunchBtn(container, existingBtn) {
-    if (!container || container.dataset.tmgUpgraded) return;
-    container.dataset.tmgUpgraded = '1';
-
-    const appName = extractAppName(container);
-    const appUrl  = existingBtn?.href || extractAppUrl(container);
-
-    const btn = document.createElement('button');
-    btn.className = 'tmg-card-launch-btn';
-    btn.textContent = '⚡ Launch with Mission Guide';
-    btn.addEventListener('click', (e) => {
-      e.preventDefault();
-      e.stopPropagation();
-      openMissionModal(appName, appUrl, '', container);
-    });
-    existingBtn.insertAdjacentElement('afterend', btn);
-  }
-
-  function extractAppName(el) {
-    const nameEl = el.querySelector('[class*="app-name"], [class*="rec-name"], h3, h4, strong');
-    if (nameEl) return nameEl.textContent.trim();
-    const btn = el.querySelector('button, a');
-    if (btn) {
-      const t = btn.textContent.replace(/OPEN|→/gi, '').trim();
-      if (t.length > 0 && t.length < 60) return t;
-    }
-    return 'Recommended App';
-  }
-
-  function extractAppUrl(el) {
-    const a = el.querySelector('a[href*=".html"]');
-    if (a) return a.href;
-    const btn = el.querySelector('button');
-    if (btn) {
-      const onclick = btn.getAttribute('onclick') || '';
-      const match = onclick.match(/['"]([^'"]*\.html[^'"]*)['"]/);
-      if (match) return match[1];
-    }
-    return null;
-  }
-
-  // ── Open mission modal & trigger AI generation ─────────────────────────────
-  function openMissionModal(appName, appUrl, rank, cardEl) {
-    const overlay = document.getElementById('tmg-mission-modal-overlay');
-    const body    = document.getElementById('tmg-modal-body');
-    const footer  = document.getElementById('tmg-modal-footer');
-    const launchBtn = document.getElementById('tmg-modal-launch-btn');
-    const subtitle  = document.getElementById('tmg-modal-subtitle');
-
-    // Reset modal state
-    body.innerHTML = `
-      <div class="tmg-modal-generating">
-        <div class="tmg-modal-gen-spinner"></div>
-        <div class="tmg-modal-gen-label">TSM Neural Core</div>
-        <div class="tmg-modal-gen-text">Analyzing anomaly...<br>Generating mission steps for <strong style="color:#c8e6c9">${escHtml(appName)}</strong>...</div>
-      </div>
-    `;
-    footer.style.display = 'none';
-    launchBtn.disabled = true;
-    subtitle.textContent = appName + ' · Generating plan...';
-    overlay.classList.add('active');
-    _pendingMission = null;
-
-    // Gather context from the War Room
-    const context = gatherWarRoomContext();
-
-    function resolveTargetApp(context) {
-  const text = ((context.documentType||'') + ' ' + (context.denialReason||'') + ' ' + (context.documentText||'')).toLowerCase();
-  const code = context.denialCode || '';
-
-  // Denial code routing
-  const denialRoutes = {
-    'CO-5':  { app:'Insurance Strategist', url:'insurance-strategist.html', reason:'Authorization & prior auth dispute requires strategist workflow' },
-    'CO-4':  { app:'Insurance Strategist', url:'insurance-strategist.html', reason:'Medical necessity appeal requires strategist review' },
-    'CO-16': { app:'Insurance Strategist', url:'insurance-strategist.html', reason:'Claim correction and resubmission via strategist' },
-    'CO-22': { app:'Insurance Strategist', url:'insurance-strategist.html', reason:'COB dispute requires payer sequencing review' },
-    'CO-97': { app:'P&C Command',          url:'pc-command.html',           reason:'Coding/bundling error requires compliance review' },
-    'PR-96': { app:'Insurance Strategist', url:'insurance-strategist.html', reason:'Patient liability determination requires strategist' },
-  };
-  if (code && denialRoutes[code]) return denialRoutes[code];
-
-  // Document type / scenario routing
-  if (/dme|oxygen|hcpcs|concentrator|wheelchair|prosthetic/.test(text))
-    return { app:'DME Billing Module', url:'dme.html', reason:'DME claim requires DME-specific billing workflow' };
-
-  if (/compliance|stark|anti.?kickback|cms|regulation|audit|rac/.test(text))
-    return { app:'P&C Command', url:'pc-command.html', reason:'Compliance issue requires P&C regulatory review' };
-
-  if (/ce |continuing education|ahip|license|licensing|certification/.test(text))
-    return { app:'CE Study Prep', url:'ce-study-prep.html', reason:'Licensing/CE issue routed to CE Study Prep' };
-
-  if (/agent|onboard|npn|appointment|carrier appointment/.test(text))
-    return { app:'Agent Onboarding', url:'ins-hub.html', reason:'Agent credentialing issue routed to onboarding workflow' };
-
-  if (/underwriting|policy lapse|cancellation|non.?renewal|rescission/.test(text))
-    return { app:'AZ Insurance Command', url:'az-ins.html', reason:'Policy lifecycle issue routed to AZ Command' };
-
-  if (/p&c|property|casualty|auto|homeowner|liability|vehicle/.test(text))
-    return { app:'P&C Command', url:'pc-command.html', reason:'P&C claim routed to P&C Command' };
-
-  if (/era|835|remittance|eob|payment posting/.test(text))
-    return { app:'Insurance Strategist', url:'insurance-strategist.html', reason:'ERA/remittance issue routed to strategist' };
-
-  if (/bnca|negotiated|contract|fee schedule/.test(text))
-    return { app:'Insurance Intel', url:'ins-intel.html', reason:'Contract/BNCA analysis routed to Insurance Intel' };
-
-  // Default
-  return { app:'Insurance Strategist', url:'insurance-strategist.html', reason:'General claim anomaly routed to strategist' };
-}
-
-    generateMissionSteps(appName, appUrl, context)
-      .then(mission => {
-        _pendingMission = mission;
-        renderModalMission(mission);
-        footer.style.display = 'flex';
-        launchBtn.disabled = false;
-        subtitle.textContent = appName + ' · Mission ready — ' + (mission.steps?.length || 0) + ' steps';
-      })
-      .catch(err => {
-        console.error('Mission generation error:', err);
-        body.innerHTML = `
-          <div class="tmg-modal-generating">
-            <div style="color:#ff6b6b;font-size:11px;text-align:center;margin-bottom:10px">⚠ Generation Failed</div>
-            <div class="tmg-modal-gen-text" style="color:#5a8a5a">Could not connect to TSM Neural Core.<br>Check server connection and retry.</div>
-          </div>
-        `;
-        footer.style.display = 'flex';
-        launchBtn.disabled = true;
-      });
-  }
-
-  function renderModalMission(m) {
-    const body = document.getElementById('tmg-modal-body');
-    const prog = m.steps?.length || 0;
-
-    body.innerHTML = `
-      <div class="tmg-modal-anomaly">
-        <div class="tmg-modal-anomaly-label">Anomaly Detected</div>
-        <div class="tmg-modal-anomaly-type">${escHtml(m.anomalyType)}</div>
-        <div class="tmg-modal-anomaly-details">${escHtml(m.anomalySummary)}</div>
-      </div>
-
-      <div class="tmg-modal-app-row">
-        <div class="tmg-modal-app-star">★</div>
-        <div class="tmg-modal-app-info">
-          <div class="tmg-modal-app-rank">Recommended App</div>
-          <div class="tmg-modal-app-name">${escHtml(m.targetApp)}</div>
-        </div>
-        <div style="color:#5a8a5a;font-size:9px">${prog} steps generated</div>
-      </div>
-
-      <div class="tmg-modal-steps-preview">
-        <div class="tmg-modal-steps-label">Mission Steps Preview</div>
-        ${(m.steps || []).map((s, i) => `
-          <div class="tmg-modal-step-row">
-            <div class="tmg-modal-step-num">${String(i+1).padStart(2,'0')}</div>
-            <div class="tmg-modal-step-content">
-              <div class="tmg-modal-step-title">${escHtml(s.title)}</div>
-              <div class="tmg-modal-step-instruction">${escHtml(s.instruction)}</div>
-              ${s.fieldHint ? `<div class="tmg-modal-step-hint">↳ ${escHtml(s.fieldHint)}</div>` : ''}
-            </div>
-          </div>
-        `).join('')}
-      </div>
-    `;
-  }
-
-  function resolveTargetApp(context) {
-    const text = ((context.documentType||'') + ' ' + (context.denialReason||'') + ' ' + (context.documentText||'')).toLowerCase();
-    const code = context.denialCode || '';
-    const denialRoutes = {
-      'CO-5':  { app:'Insurance Strategist', url:'insurance-strategist.html', reason:'Authorization & prior auth dispute' },
-      'CO-4':  { app:'Insurance Strategist', url:'insurance-strategist.html', reason:'Medical necessity appeal' },
-      'CO-16': { app:'Insurance Strategist', url:'insurance-strategist.html', reason:'Claim correction and resubmission' },
-      'CO-22': { app:'Insurance Strategist', url:'insurance-strategist.html', reason:'COB dispute — payer sequencing' },
-      'CO-97': { app:'P&C Command',          url:'pc-command.html',           reason:'Coding/bundling error — compliance review' },
-      'PR-96': { app:'Insurance Strategist', url:'insurance-strategist.html', reason:'Patient liability determination' },
-    };
-    if (code && denialRoutes[code]) return denialRoutes[code];
-    if (/dme|oxygen|hcpcs|concentrator|wheelchair|prosthetic/.test(text))
-      return { app:'DME Billing Module', url:'dme.html', reason:'DME claim requires DME billing workflow' };
-    if (/compliance|stark|anti.?kickback|cms|regulation|audit|rac/.test(text))
-      return { app:'P&C Command', url:'pc-command.html', reason:'Compliance issue — P&C regulatory review' };
-    if (/ce |continuing education|ahip|license|licensing|certification/.test(text))
-      return { app:'CE Study Prep', url:'ce-study-prep.html', reason:'Licensing/CE issue' };
-    if (/agent|onboard|npn|appointment|carrier appointment/.test(text))
-      return { app:'Agent Onboarding', url:'ins-hub.html', reason:'Agent credentialing issue' };
-    if (/underwriting|policy lapse|cancellation|non.?renewal|rescission/.test(text))
-      return { app:'AZ Insurance Command', url:'az-ins.html', reason:'Policy lifecycle issue' };
-    if (/p&c|property|casualty|auto|homeowner|liability|vehicle/.test(text))
-      return { app:'P&C Command', url:'pc-command.html', reason:'P&C claim' };
-    if (/era|835|remittance|eob|payment posting/.test(text))
-      return { app:'Insurance Strategist', url:'insurance-strategist.html', reason:'ERA/remittance issue' };
-    if (/bnca|negotiated|contract|fee schedule/.test(text))
-      return { app:'Insurance Intel', url:'ins-intel.html', reason:'Contract/BNCA analysis' };
-    return { app:'Insurance Strategist', url:'insurance-strategist.html', reason:'General claim anomaly' };
-  }
-
-  // ── Generate steps via TSM Neural Core ────────────────────────────────────
-  async function generateMissionSteps(appName, appUrl, context) {
-    const missionId =
-  ((window.location.pathname.match(/\/([^\/]+)-war-room/i)?.[1] || 'TSM')
-    .toUpperCase()) +
-  '-' +
-  Date.now().toString(36).toUpperCase();
-
-    // Override app routing based on anomaly intelligence
-    const route = resolveTargetApp(context);
-    if (route.url !== appUrl) {
-      appName = route.app;
-      appUrl = route.url;
-    }
-
-    const systemPrompt = `You are TSM Neural Core, an expert insurance operations AI. 
-You generate precise guided mission steps for insurance staff to resolve anomalies using specific apps.
-Respond ONLY with valid JSON. No markdown, no explanation, no code fences.`;
-
-    const userPrompt = `Generate a step-by-step guided mission for this insurance anomaly resolution task.
-
-TARGET APP: ${appName}
-ANOMALY TYPE: ${context.documentType || 'Insurance Claim'}
-DOCUMENT/CLAIM CONTEXT:
-${context.documentText || 'No document loaded'}
-
-CURRENT ANALYSIS CONTEXT:
-Claim Amount: ${context.claimAmount || 'Unknown'}
-Patient/Policy: ${context.patient || 'Unknown'}
-Status: ${context.status || 'Unknown'}
-Denial Reason: ${context.denialReason || 'Not specified'}
-
-Generate 5-7 actionable mission steps. Each must include:
-- title: short action title (max 7 words)
-- instruction: clear 1-2 sentence instruction of exactly what to do in the ${appName} app
-- fieldHint: specific field names, exact values, or inputs to enter (e.g., "In the Denial Analysis tab, set Denial Code to CO-16, enter ICD-10: 396.00 in the Diagnosis field, enter Claim Amount: $3,200")
-
-Also output:
-- anomalyType: one-line label for what the anomaly is
-- anomalySummary: 2-3 sentence summary of what happened and why this app was recommended
-
-Respond with ONLY this JSON structure:
-{
-  "anomalyType": "...",
-  "anomalySummary": "...",
-  "steps": [
-    { "title": "...", "instruction": "...", "fieldHint": "..." }
-  ]
-}`;
-
-    const res = await fetch('/api/insurance/query', {
-      method: 'POST',
-      headers: { 'Content-Type': 'application/json' },
-      body: JSON.stringify({ system: systemPrompt, message: userPrompt })
-    });
-
-    if (!res.ok) throw new Error('API error: ' + res.status);
-
-    const data = await res.json();
-    const rawText = (data.response || data.content || data.result || '').replace(/```json|```/g, '').trim();
-    const parsed = JSON.parse(rawText);
-
-    return {
-      missionId,
-      anomalyType: parsed.anomalyType || context.documentType || 'Insurance Anomaly',
-      anomalySummary: parsed.anomalySummary || '',
-      targetApp: appName,
-      targetUrl: appUrl,
-      patientOrClient: context.patient,
-      claimAmount: context.claimAmount,
-      denialCode: context.denialCode || null,
-      payer: context.payer || null,
-      steps: (parsed.steps || []).map((s, i) => ({
-        id: `step-${i}`,
-        title: s.title,
-        instruction: s.instruction,
-        fieldHint: s.fieldHint || '',
-        status: i === 0 ? 'active' : 'pending'
-      })),
-      generatedAt: new Date().toISOString(),
-      warRoomContext: context
-    };
-  }
-
-  // ── Gather context from the War Room DOM ──────────────────────────────────
-  function gatherWarRoomContext() {
-    const ctx = {};
-
-    // Document type button that is active/selected
-    const activeDocType = document.querySelector(`
-  .doc-type-btn.active,
-  .doc-type-btn.selected,
-  .sample-btn.active,
-  .type-btn.active,
-  .guide-btn.active,
-  .doc-btn.active,
-  [data-doc-type].active,
-  [data-type].active,
-  button.active[data-type]
-`);
-
-    ctx.documentType = activeDocType?.textContent?.trim()
-      || document.querySelector('#selectedDocType, [id*="docType"]')?.textContent?.trim()
-      || 'AP Aging';
-
-    // Pasted document text
-    const docTextArea = document.querySelector(`
-  textarea[id*="doc"],
-  textarea[id*="paste"],
-  textarea[id*="text"],
-  textarea[id*="input"],
-  textarea[id*="claim"],
-  textarea[id*="contract"],
-  textarea[id*="legal"],
-  textarea[id*="financial"],
-  textarea[placeholder*="Paste"],
-  textarea[placeholder*="Document"],
-  #documentText,
-  #pasteArea,
-  #docText,
-  #inputText,
-  #sourceText
-`);
-    ctx.documentText = docTextArea?.value?.trim()?.slice(0, 1000) || '';
-
-    // Try to extract claim details from the text
-    if (ctx.documentText) {
-      const amtMatch = ctx.documentText.match(/CLAIM[\s_]?AMOUNT[:\s]+(\$[\d,]+)/i)
-        || ctx.documentText.match(/TOTAL[\s_]?AMOUNT[:\s]+(\$[\d,]+)/i)
-        || ctx.documentText.match(/(\$[\d,]+)/);
-      if (amtMatch) ctx.claimAmount = amtMatch[1];
-
-      const patMatch = ctx.documentText.match(/PATIENT[:\s]+([^\n]+)/i)
-        || ctx.documentText.match(/POLICY[\s_]?HOLDER[:\s]+([^\n]+)/i)
-        || ctx.documentText.match(/MEMBER[:\s]+([^\n]+)/i);
-      if (patMatch) ctx.patient = patMatch[1].trim();
-
-      const statusMatch = ctx.documentText.match(/STATUS[:\s]+([^\n]+)/i);
-      if (statusMatch) ctx.status = statusMatch[1].trim();
-
-      const denialMatch = ctx.documentText.match(/DENIAL[\s_]?REASON[:\s]+([^\n]+)/i)
-        || ctx.documentText.match(/DENIAL[:\s]+([^\n]+)/i)
-        || ctx.documentText.match(/REASON[\s_]?FOR[\s_]?DENIAL[:\s]+([^\n]+)/i);
-      if (denialMatch) ctx.denialReason = denialMatch[1].trim();
-
-      // Extract denial code (CO-5, PR-96, OA-23, etc.)
-      const codeMatch = ctx.documentText.match(/\b(CO-\d+|PR-\d+|OA-\d+|PI-\d+|CR-\d+)\b/i)
-        || (ctx.denialReason||'').match(/\b(CO-\d+|PR-\d+|OA-\d+|PI-\d+|CR-\d+)\b/i);
-      if (codeMatch) ctx.denialCode = codeMatch[1].toUpperCase();
-
-      // Extract payer
-      const payerMatch = ctx.documentText.match(/PAYER[:\s]+([^\n]+)/i)
-        || ctx.documentText.match(/INSURANCE[:\s]+([^\n]+)/i)
-        || ctx.documentText.match(/\b(Medicare|Medicaid|Blue Cross|Aetna|Cigna|UnitedHealth|Humana|Anthem|Centene)\b/i);
-      if (payerMatch) ctx.payer = payerMatch[1].trim();
-    }
-
-    // Fallback: scrape denial code from visible engine output panels
-    if (!ctx.denialCode) {
-      const allText = document.body.innerText || '';
-      const codeMatch = allText.match(/\b(CO-\d+|PR-\d+|OA-\d+|PI-\d+)\b/i);
-      if (codeMatch) ctx.denialCode = codeMatch[1].toUpperCase();
-    }
-
-    // Fallback: scrape payer from visible DOM
-    if (!ctx.payer) {
-      const allText = document.body.innerText || '';
-      const payerMatch = allText.match(/\b(Medicare|Medicaid|Blue Cross|Aetna|Cigna|UnitedHealth|Humana|Anthem|Centene)\b/i);
-      if (payerMatch) ctx.payer = payerMatch[1];
-    }
-
-    // Fallback claimAmount from status bar
-    if (!ctx.claimAmount) {
-      const statusBar = document.querySelector('[id*="statusBar"], .status-bar, #engineStatus');
-      ctx.claimAmount = statusBar?.textContent?.match(/\$[\d,]+/)?.[0] || '';
-    }
-
-    return ctx;
-  }
-
-  // ── Modal button handlers ──────────────────────────────────────────────────
-  document.addEventListener('DOMContentLoaded', function () {
-    document.getElementById('tmg-modal-close-btn')?.addEventListener('click', closeMissionModal);
-    document.getElementById('tmg-modal-cancel-btn')?.addEventListener('click', closeMissionModal);
-    document.getElementById('tmg-mission-modal-overlay')?.addEventListener('click', function(e) {
-      if (e.target === this) closeMissionModal();
-    });
-
-    document.getElementById('tmg-modal-launch-btn')?.addEventListener('click', function () {
-      if (!_pendingMission) return;
-      // Write mission to localStorage — the guide panel in the target app will read it
-      try {
-        localStorage.setItem(STORAGE_KEY, JSON.stringify(_pendingMission));
-if(window.TSMMissionGuide) window.TSMMissionGuide.show();
-      } catch(e) {}
-      closeMissionModal();
-
-      // Navigate to target app
-      if (_pendingMission.targetUrl) {
-        window.location.href = _pendingMission.targetUrl;
-      } else {
-        // Guide is now loaded — alert user the panel is ready
-        if (window.TSMMissionGuide) window.TSMMissionGuide.show();
-      }
-    });
-
-    // Upgrade recommendation cards after a short delay (let War Room render first)
-    setTimeout(upgradeRecommendationCards, 600);
-
-    // Also re-run after engine analysis completes (if War Room fires a custom event)
-    document.addEventListener('warRoomAnalysisComplete', upgradeRecommendationCards);
-    document.addEventListener('enginesComplete', upgradeRecommendationCards);
-
-    // MutationObserver to catch dynamically rendered cards
-    const observer = new MutationObserver(() => {
-      upgradeRecommendationCards();
-    });
-    const recContainer = document.querySelector(
-      '#recommendationsContainer, .recommendations, [id*="rec"], [class*="rec-container"]'
-    );
-    if (recContainer) {
-      observer.observe(recContainer, { childList: true, subtree: true });
-    }
-  });
-
-  function closeMissionModal() {
-    document.getElementById('tmg-mission-modal-overlay')?.classList.remove('active');
-    _pendingMission = null;
-  }
-
-  function escHtml(str) {
-    return String(str || '').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;');
-  }
- 
-})();
-</script>
-<script src="/html/tsm-insurance/public/js/tsm-mission-guide.js"></script>
 <script src="../..//js/tsm-mission-conductor.js"></script>
 <script src="/html/tsm-mission-orchestrator.js"></script>
 <script src="/html/core/tsm-enforcer.js"></script>
diff --git a/server.js b/server.js
index 907cb45e..571b6ddf 100644
--- a/server.js
+++ b/server.js
@@ -293,7 +293,7 @@ app.use('/html', express.static(path.join(__dirname, 'html'), { setHeaders: (res
 app.use('/js', express.static(path.join(__dirname, 'html/tsm-insurance/public/js')));
 app.use('/js', express.static(path.join(__dirname, 'html/js')));
 app.use('/bpo', express.static(path.join(__dirname, 'html/bpo')));
-app.use('/shared', express.static(path.join(__dirname, 'html/bpo/shared')));
+app.use('/shared', express.static(path.join(__dirname, 'html/shared')));
 app.use('/insurance', express.static(path.join(__dirname, 'html/tsm-insurance')));
 app.use('/construction', express.static(path.join(__dirname, 'html/construction-suite')));
 // NOTE: /runtime and /architecture mounts now live earlier in this file
