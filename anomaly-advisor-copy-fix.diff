--- html/healthcare/hc-denial-war-room.html	2026-08-15 02:28:58.517418365 +0000
+++ /tmp/hc-denial-war-room-test.html	2026-08-15 04:43:27.908628411 +0000
@@ -2172,9 +2172,10 @@
  
   const body = document.getElementById('anomaly-panel-body');
   const context = `ENGINE 1 (DOCUMENT INTEL):\n${(outputs[0]||'').slice(0,400)}\n\nENGINE 2 (ROOT CAUSE):\n${(outputs[1]||'').slice(0,400)}\n\nENGINE 3 (FINANCIAL):\n${(outputs[2]||'').slice(0,300)}\n\nENGINE 4 (RECOVERY):\n${(outputs[3]||'').slice(0,300)}\n\nDOC SAMPLE:\n${(docText||'').slice(0,500)}`;
-  const sys = `You are TSM Anomaly Advisor, a field-level defect detection engine for healthcare RCM documents. Analyze the engine outputs and identify specific actionable document defects. Output ONLY a numbered list. Each item must follow this exact format:
-⚠ [FIELD NAME] — what is wrong — exact fix instruction (which tab, which field, what to enter or change).
-Maximum 8 items. No preamble. No headers. No summary. Start immediately with item 1.`;
+  const sys = `You are TSM Anomaly Advisor, a document-quality reviewer for healthcare RCM claims. Analyze the engine outputs and identify the defects preventing a clean appeal. Output ONLY a numbered list, ordered by priority — most urgent or highest-dollar-impact issue first. Each item must follow this exact format:
+⚠ [Issue] — what's missing or incorrect — the specific correction needed and who on the team should make it (e.g. billing coordinator, provider, coder, RCM manager).
+Describe each fix in plain terms a revenue cycle team member would act on directly. Do not reference application tabs, fields, or menu names — those vary by system and this list should work no matter what software the reader uses.
+Maximum 6 items. No preamble. No headers. No summary. Start immediately with item 1.`;
  
   let anomalyFullText = '';
   let firstChunk = true;
