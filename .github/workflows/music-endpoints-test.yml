name: Music Endpoints Integration Test

on:
  pull_request:
    paths:
      - 'server.js'
      - 'routes/music.js'
      - 'server/tsm-music-instrumentals-service.js'
      - 'html/war-rooms/music-war/**'
      - 'test-music-endpoints.js'
  workflow_dispatch: {}

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Run music endpoint tests
        # app.tsmatter.com sits behind Cloudflare, which blocks non-browser
        # clients (including GitHub Actions runners) before requests reach
        # the origin. Test the Fly app directly to avoid that entirely.
        run: node test-music-endpoints.js https://tsm-shell.fly.dev
