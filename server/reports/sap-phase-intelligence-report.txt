TSM Enterprise SAP Phase Intelligence

STATUS:
NOT INSTALLED

This report file previously said "STATUS: READY" unconditionally --
that text was hardcoded into apply-enterprise-sap-phase-intelligence.sh's
final heredoc and printed regardless of whether anything was actually
created. Verified against this checkout on 2026-08-05:

  - html/shared/runtime/enterprise/  -- does not exist
  - data/enterprise-lab/             -- does not exist
  - sap-phase-intelligence.js, sap-process-router.js,
    sap-phase-manifest.json, sap-phase-library.json -- none exist
    anywhere in this repo
  - window.TSMSAPPhaseIntelligence / window.TSMSAPProcessRouter --
    referenced nowhere in any HTML or JS file in this repo

None of the "CONNECTED TO" claims below were ever true in this
checkout. Do not cite this report, or the P2P/O2C/R2R/MDM/WIP mission
classifier it describes, as an existing capability in a pilot pitch.

WHAT IS REAL INSTEAD:
Enterprise capability scoring (o2c, crm, cpq, catalog, approval, mdm,
integration, governance, wip, digital-twin) is live and working via
server/enterprise/enterprise-engine.js -> enterprise-orchestrator.js,
called from POST /api/enterprise/{dashboard,decision,missions}. Each
vertical gets all 10 modules, relabeled into that vertical's own
language by domain-map.js (see that file's header for why relabeling,
not filtering, is the deliberate design).

If the classifier described below is still wanted, the script
(scripts/apply-enterprise-sap-phase-intelligence.sh) has working code
ready to install -- it just was never run and committed in this repo.
Before installing it: decide whether a second, separate P2P/O2C/R2R/
MDM/WIP classification actually adds anything beyond what domain-map.js
+ the 10 capability modules already provide, since they cover
overlapping ground (O2C, MDM, and WIP already exist as real modules).

ORIGINAL ASPIRATIONAL CONTENT (never installed, kept for reference):

CONNECTED TO (claimed, not verified true):
Mission Queue
Enterprise Runtime
War Rooms
Executive Command Center
Sentinel

SAP PHASES (as designed, not installed):
P2P
O2C
R2R
MDM
WIP

Architecture (as designed, not installed):
Document
   |
Mission
   |
SAP Phase Detection
   |
War Room
   |
Executive Decision
   |
Sentinel
