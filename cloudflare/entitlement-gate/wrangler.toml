name = "tsm-entitlement-gate"
main = "worker.js"
compatibility_date = "2024-01-01"

# One route entry covers every current and future subdomain.
# zone_name must match exactly what's in your Cloudflare dashboard.
[[routes]]
pattern = "*.tsmatter.com/*"
zone_name = "tsmatter.com"
