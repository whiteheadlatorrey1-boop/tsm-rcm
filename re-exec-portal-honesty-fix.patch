
diff --git a/html/war-rooms/re-war/re-exec-portal.html b/html/war-rooms/re-war/re-exec-portal.html
index 138eee7b..7abe8419 100644
--- a/html/war-rooms/re-war/re-exec-portal.html
+++ b/html/war-rooms/re-war/re-exec-portal.html
@@ -1494,7 +1494,7 @@ async function extractRelayIntelligence() {
       method: 'POST',
       headers: { 'Content-Type': 'application/json' },
       body: JSON.stringify({
-        question: `Extract structured KPIs from this real estate/mortgage relay data. Respond with ONLY a single-line JSON object — no markdown, no commentary, no backticks: {"headline":"8-12 word alert citing the single biggest dollar risk or issue with exact dollar figure","riskLevel":"LOW or MEDIUM or HIGH","riskScore":integer 0-100,"dollarRisk":"$X total exposure","estRevenue":"$X potential revenue impact or savings","reoCount":integer or null,"reoValue":"$X or null","closingsCount":integer or null,"loansActive":integer or null,"tridViolations":integer or null,"titleIssuesOpen":integer or null}. Data: ${ctxText}`,
+        question: `Extract structured KPIs from this real estate/mortgage relay data. Only use a value when it is directly derivable from the data below — if you cannot confidently compute a real figure for a field, respond with null for that field. Never guess or default to $0 or any placeholder number. Respond with ONLY a single-line JSON object — no markdown, no commentary, no backticks: {"headline":"8-12 word alert citing the single biggest dollar risk or issue with exact dollar figure, or null if none can be confidently derived","riskLevel":"LOW or MEDIUM or HIGH, or null if not derivable","riskScore":"integer 0-100 or null","dollarRisk":"$X total exposure, or null if not derivable","estRevenue":"$X potential revenue impact or savings, or null if not derivable","reoCount":integer or null,"reoValue":"$X or null","closingsCount":integer or null,"loansActive":integer or null,"tridViolations":integer or null,"titleIssuesOpen":integer or null}. Data: ${ctxText}`,
         maxTokens: 280
       })
     });
@@ -1536,15 +1536,16 @@ function buildTicker() {
     const fromWhere = state.warPayload.platform || 'RE Strategist';
     items.push({ cls: 'warn', text: `◉ RELAY RECEIVED · ${fromWhere.toUpperCase()} · <span>${relayTime}</span> · CHAIN ACTIVE` });
   }
-  items.push(
-    { cls: 'alert', text: state.relayHeadline ? `⚠ ${state.relayHeadline}` : 'APPRAISAL GAP ALERT · 123 MAPLE ST · CONTRACT $460K · APPRAISED $434K · GAP: <span>$26,000</span>' },
-    { cls: 'warn', text: 'REO PIPELINE · 6 ACTIVE ASSETS · <span>$2.1M PORTFOLIO</span> · 2 READY FOR LISTING' },
-    { cls: '', text: 'PULL-THROUGH: <span>68%</span> · TARGET: 75% · DELTA: -7PT · RECOVERY PLAN ACTIVE' },
-    { cls: 'alert', text: 'TRID VIOLATION · LOAN #2847 · <span>$1,247 CURE REQUIRED</span> · WINDOW: 18 HOURS' },
-    { cls: '', text: 'CLOSINGS THIS WEEK: <span>5</span> · $2.3M FUNDED · PHOENIX METRO · ON TRACK' },
-    { cls: 'warn', text: 'AML REVIEW · <span>2 LOANS PENDING</span> · FinCEN BENEFICIAL OWNERSHIP · COMPLIANCE HOLD' },
-    { cls: '', text: 'DSCR LOAN · 2200 SUNRISE BLVD · $625K · INVESTOR PACKAGE UNDER REVIEW' },
-  );
+  if (state.relayHeadline) {
+    items.push({ cls: 'alert', text: `⚠ ${state.relayHeadline}` });
+  } else if (items.length === 0) {
+    // No relay timestamp and no AI-derived headline — nothing real to show
+    items.push({ cls: '', text: 'NO LIVE ALERT DATA · awaiting relay analysis' });
+  }
+  if (items.length === 0) {
+    document.getElementById('ticker-content').innerHTML = '';
+    return;
+  }
   const doubled = [...items, ...items];
   const html = doubled.map(i =>
     `<div class="tick-item ${i.cls}">${i.text}</div>`
