diff --git a/demo/realestate-demo.json b/demo/realestate-demo.json
index cf4ca536..ce4ba99f 100644
--- a/demo/realestate-demo.json
+++ b/demo/realestate-demo.json
@@ -4,7 +4,7 @@
   "steps": [
     { "shot": "001-war-room-load", "goto": "/html/war-rooms/re-war/re-war-room.html", "waitMs": 1500 },
 
-    { "shot": "002-quick-fire-compliance", "click": "button[onclick*=\"quickFire('Run a compliance sweep\"]", "waitFor": "#feedBody", "waitMs": 3500 },
+    { "shot": "002-quick-fire-compliance", "click": "div.quick-link[onclick*=\"quickFire('Run a compliance sweep\"]", "waitFor": "#feedBody", "waitMs": 3500 },
 
     { "shot": "003-feed-output", "waitFor": "#feedMeta", "waitMs": 500 },
 
diff --git a/html/war-rooms/re-war/re-exec-portal.html b/html/war-rooms/re-war/re-exec-portal.html
index 5b6c2ddd..651a68b9 100644
--- a/html/war-rooms/re-war/re-exec-portal.html
+++ b/html/war-rooms/re-war/re-exec-portal.html
@@ -118,7 +118,6 @@ window.tsmEnforcer = window.TSMEnforcer;
 (function() {
   function nukeAssistant() {
     const selectors = [
-      '',
       '.assistant-launcher',
       '.chat-widget-container',
       '.bot-avatar-btn',
@@ -2636,82 +2635,6 @@ function tsmWriteRelay(payload){
 </script>
 
 
-<!-- DYNAMIC INTERACTIVE GUIDE FOR EU: RE-EXEC-PORTAL -->
-
-    <div id="exec-step-2" style="margin-bottom: 8px; color: #64748b; opacity: 0.6; display: flex; align-items: flex-start; gap: 8px;">
-      <span class="step-icon" style="font-weight: bold;">○</span>
-      <div>
-        <strong>2. Generate Executive Brief</strong>
-        <div style="font-size: 10px; color: #94a3b8;">Click <span style="color: #10b981;">"⚡ GENERATE BRIEF"</span> or review active FinCEN/TRID Compliance Flags.</div>
-      </div>
-    </div>
-    <div id="exec-step-3" style="margin-bottom: 4px; color: #64748b; opacity: 0.6; display: flex; align-items: flex-start; gap: 8px;">
-      <span class="step-icon" style="font-weight: bold;">○</span>
-      <div>
-        <strong>3. Execute Sign-off or Deal Rescue</strong>
-        <div style="font-size: 10px; color: #94a3b8;">Trigger <span style="color: #f43f5e;">"● DEAL RESCUE"</span> to deploy mitigation overrides.</div>
-      </div>
-    </div>
-  </div>
-  <div style="background: #030712; padding: 8px 12px; border-top: 1px solid #1e293b; font-size: 10px; color: #f59e0b;" id="exec-guide-hint">
-    <strong>Next:</strong> Select a Deal Snapshot or Quick Analysis option from sidebar.
-  </div>
-</div>
-
-<script>
-(function() {
-  let currentStep = 1;
-
-  function completeStep(stepNum, nextHint) {
-    if (stepNum < currentStep) return;
-    
-    const stepEl = document.getElementById("exec-step-" + stepNum);
-    if (stepEl) {
-      stepEl.style.color = "#10b981";
-      stepEl.style.opacity = "1";
-      stepEl.querySelector(".step-icon").innerHTML = "✓";
-    }
-
-    currentStep = stepNum + 1;
-    const nextEl = document.getElementById("exec-step-" + currentStep);
-    if (nextEl) {
-      nextEl.style.color = "#f59e0b";
-      nextEl.style.opacity = "1";
-      nextEl.querySelector(".step-icon").innerHTML = "●";
-      document.getElementById("exec-guide-step-counter").innerText = "STEP " + currentStep + " OF 3";
-    }
-
-    if (nextHint) {
-      document.getElementById("exec-guide-hint").innerHTML = "<strong>Next:</strong> " + nextHint;
-    }
-  }
-
-  // Hook event listeners for step progress
-  document.addEventListener("click", function(e) {
-      const el = e.target.closest("*");
-      if (!el) return;
-      const txt = (el.innerText || "").toUpperCase();
-
-      // Step 1: Select deal/case item
-      if (txt.includes("DEAL") || txt.includes("COMPLIANCE") || txt.includes("REO") || txt.includes("PORTFOLIO") || txt.includes("SNAPSHOT") || txt.includes("FINCEN") || txt.includes("TRID")) {
-        markComplete(1, "Click <strong>⚡ GENERATE BRIEF</strong> to compile executive metrics.");
-      }
-      
-      // Step 2: Generate brief
-      if (txt.includes("GENERATE BRIEF") || txt.includes("EXECUTIVE BRIEF") || txt.includes("BRIEF")) {
-        markComplete(1);
-        markComplete(2, "Click <strong>● DEAL RESCUE</strong> to execute sign-off or override.");
-      }
-      
-      // Step 3: Deal Rescue / Sign-off
-      if (txt.includes("DEAL RESCUE") || txt.includes("RESCUE PLAN") || txt.includes("SIGN-OFF")) {
-        markComplete(1);
-        markComplete(2);
-        markComplete(3, "Executive Sign-off complete. Strategic posture saved.");
-      }
-    }, true);
-})();
-</script>
 
 
 
diff --git a/html/war-rooms/re-war/re-strategist.html b/html/war-rooms/re-war/re-strategist.html
index 975aeff8..fde980a9 100644
--- a/html/war-rooms/re-war/re-strategist.html
+++ b/html/war-rooms/re-war/re-strategist.html
@@ -362,8 +362,10 @@ body::before{content:'';position:fixed;inset:0;
     }
 </style>
 
+<style>
 /* Permanent killswitch for AI Chat Assistant overlay */
 .assistant-launcher, .chat-widget-container { display: none !important; visibility: hidden !important; pointer-events: none !important; }
+</style>
 </head>
 <body>
 
diff --git a/html/war-rooms/re-war/re-war-room.html b/html/war-rooms/re-war/re-war-room.html
index 8ca6140f..77396759 100644
--- a/html/war-rooms/re-war/re-war-room.html
+++ b/html/war-rooms/re-war/re-war-room.html
@@ -1578,8 +1578,10 @@
   
   <script src="/js/core/tsm-auto-pipeline.js"></script>
 
+<style>
 /* Permanent killswitch for AI Chat Assistant overlay */
 #tsm-mission-guide-panel, .assistant-launcher, .chat-widget-container { display: none !important; visibility: hidden !important; pointer-events: none !important; }
+</style>
 </head>
 
 <body>
diff --git a/tests/e2e/demo/realestate-demo.spec.js b/tests/e2e/demo/realestate-demo.spec.js
index 97317433..cebb4e5a 100644
--- a/tests/e2e/demo/realestate-demo.spec.js
+++ b/tests/e2e/demo/realestate-demo.spec.js
@@ -9,6 +9,14 @@ test('Real Estate executive demo', async ({ page }) => {
   await page.setViewportSize({ width: 1920, height: 1080 });
   test.setTimeout(120_000);
 
+  // The strategist page has an undocumented auto-escalation timer that fires
+  // 1800ms after load and jumps straight to the Exec Portal unless this flag
+  // is set. Without it, the manual "Full Strategic Brief" click races the
+  // auto-chain and the recording skips the brief step entirely.
+  await page.addInitScript(() => {
+    localStorage.setItem('tsm_auto_mode', 'off');
+  });
+
   const story = loadStory(path.join(__dirname, '../../../demo/realestate-demo.json'));
   const outDir = path.join(__dirname, 'screenshots', 'realestate');
 
