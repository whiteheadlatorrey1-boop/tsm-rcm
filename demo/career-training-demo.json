{
  "vertical": "career-training",
  "note": "Selectors verified 2026-08-10 against whiteheadlatorrey1-boop/tsm-rcm (html/tsm-career-training-platform.html). This is a launcher/reference hub (switchTo(id) shows #panel-<id>, hides the rest), not a single war-room flow, so the story walks the real nav path: overview -> hc sector suite -> crcr deep dive -> readiness tracker -> hitl approval -> deployment. Do not reuse screenshots across steps; if a click fails, the engine names the file *-CLICK-FAILED.png instead of silently reusing the prior shot.",
  "steps": [
    { "shot": "001-platform-load", "goto": "/html/tsm-career-training-platform.html", "waitFor": "#panel-overview", "waitMs": 1200 },

    { "shot": "002-select-hc-sector", "click": ".nav-item[data-panel=\"hc\"]", "waitFor": "#panel-hc.active", "waitMs": 800 },

    { "shot": "003-hc-suite", "waitMs": 500 },

    { "shot": "004-crcr-deep-dive", "click": ".nav-item[data-panel=\"crcr\"]", "waitFor": "#panel-crcr.active", "waitMs": 800 },

    { "shot": "005-readiness-tracker", "click": ".nav-item[data-panel=\"readiness\"]", "waitFor": "#panel-readiness.active", "waitMs": 800 },

    { "shot": "006-hitl-approval", "click": ".nav-item[data-panel=\"hitl\"]", "waitFor": "#panel-hitl.active", "waitMs": 800 },

    { "shot": "007-deployment", "click": ".nav-item[data-panel=\"deployment\"]", "waitFor": "#panel-deployment.active", "waitMs": 800 }
  ]
}
