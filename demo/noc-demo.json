{
  "vertical": "noc",
  "note": "Source-verified 2026-08-08 against whiteheadlatorrey1-boop/tsm-rcm by reading real ids/onclick handlers/functions directly in html/l1-copilot/noc/{noc-war-room,noc-strategist,noc-executive-portal}.html and services/noc-engine.js. War room -> strategist -> exec portal, same 3-stage pattern as insurance/plant-incident. relayToStrategist() writes TSM.relay.write('NOC', payload) then window.location.href navigates after a 500ms setTimeout -- no separate goto step needed, matching plant-incident's escalateToStrategist()/escalateExec() pattern. Strategist's 'Executive View' link and the empty-state 'Go to War Room' link share the class 'hbtn primary' but never render simultaneously (empty-state only shows with no relay data, which never happens in this story since we always relay real data first) -- disambiguated by href anyway to be safe. runAnalysis() is a single fetch to /api/noc/query (confirmed real route in server.js), not a multi-engine chain like plant-incident's 6-engine analysis, so no long timeout needed on that step.",
  "presetLocalStorage": { "noc_tour_done": "1" },
  "steps": [
    { "shot": "001-war-room-load", "goto": "/html/l1-copilot/noc/noc-war-room.html", "waitMs": 1500 },

    { "shot": "002-load-sample-data", "click": "#btnLoadSample", "waitFor": "#kpiGrid .kpi-card", "waitMs": 800 },

    { "shot": "003-run-ai-analysis", "click": "#btnRunAnalysis", "waitFor": "#aiOutput", "waitMs": 2000, "timeout": 45000 },

    { "shot": "004-relay-to-strategist", "click": "#btnRelay", "waitFor": ".sec-hdr", "waitMs": 1500 },

    { "shot": "005-escalate-to-executive", "click": "a.hbtn.primary[href=\"noc-executive-portal.html\"]", "waitFor": ".fin-card.hero", "waitMs": 1500 },

    { "shot": "006-executive-kpis", "waitMs": 800 }
  ]
}
