diff --git a/html/war-rooms/bpo/bpo-strategist.html b/html/war-rooms/bpo/bpo-strategist.html
index eab92af..8cd4a24 100644
--- a/html/war-rooms/bpo/bpo-strategist.html
+++ b/html/war-rooms/bpo/bpo-strategist.html
@@ -225,8 +225,8 @@ body::before{content:'';position:fixed;inset:0;background:repeating-linear-gradi
   <script src="/shared/tsm-exceptions.js"></script>
   <script src="/js/core/tsm-event-bus.js"></script>
   <script src="/js/core/tsm-state.js"></script>
-  <script src="/html/shared/runtime/mission/mission-model.js"></script>
-  <script src="/html/shared/runtime/mission/mission-store.js"></script>
+  <script src="../../shared/runtime/mission/mission-model.js"></script>
+  <script src="../../shared/runtime/mission/mission-store.js"></script>
   <script src="/js/core/tsm-mission-engine.js"></script>
   <script src="/js/core/tsm-auto-pipeline.js"></script>
 </head>
@@ -355,6 +355,7 @@ body::before{content:'';position:fixed;inset:0;background:repeating-linear-gradi
         <div class="brief-tab" onclick="showBriefTab('sla')">SLA REPORT</div>
         <div class="brief-tab" onclick="showBriefTab('clients')">CLIENT IMPACT</div>
         <div class="brief-tab" onclick="showBriefTab('escalations')">ESCALATIONS</div>
+        <div class="brief-tab" onclick="showBriefTab('mission')">MISSION TIMELINE</div>
       </div>
       <div class="brief-action-strip">
         <button class="brief-act fire" id="fireStratBtn" onclick="fireStrategy()">⚡ GENERATE STRATEGY BRIEF</button>
@@ -991,10 +992,33 @@ function renderFallbackRec() {
 
 // ── BRIEF TABS ──
 function showBriefTab(tab) {
-  document.querySelectorAll('.brief-tab').forEach((el,i)=>el.classList.toggle('active',['brief','sla','clients','escalations'][i]===tab));
+  document.querySelectorAll('.brief-tab').forEach((el,i)=>el.classList.toggle('active',['brief','sla','clients','escalations','mission'][i]===tab));
   if (tab === 'sla') pullSLAReport();
   if (tab === 'clients') pullClientBrief();
   if (tab === 'escalations') pullEscalations();
+  if (tab === 'mission') showMissionTimeline();
+}
+
+function showMissionTimeline(){
+  const body = document.getElementById('briefBody');
+  const mission = window.tsmMission;
+  if (!mission || !Array.isArray(mission.audit)) {
+    body.innerHTML = '<div class="brief-section-head">MISSION TIMELINE</div><div style="color:var(--muted);font-family:JetBrains Mono,monospace;font-size:.72rem">No mission loaded — relay a case in from the War Room first.</div>';
+    return;
+  }
+  const esc = (s) => String(s).replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
+  const rows = mission.audit.map(entry => {
+    const at = entry.at ? new Date(entry.at).toLocaleString() : '—';
+    const meta = entry.meta && Object.keys(entry.meta).length ? esc(JSON.stringify(entry.meta)) : '';
+    return '<div style="border-left:2px solid var(--cyan);padding:4px 0 8px 12px;margin-bottom:6px">'
+      + '<div style="color:var(--cyan);font-family:JetBrains Mono,monospace;font-size:.7rem;font-weight:600">' + esc(entry.event) + '</div>'
+      + '<div style="color:#bbb;font-family:JetBrains Mono,monospace;font-size:.65rem">' + esc(at) + ' · actor: ' + esc(entry.actor || 'system') + '</div>'
+      + (meta ? '<div style="color:var(--muted);font-family:JetBrains Mono,monospace;font-size:.62rem;word-break:break-all">' + meta + '</div>' : '')
+      + '</div>';
+  }).join('');
+  body.innerHTML = '<div class="brief-section-head">MISSION TIMELINE</div>'
+    + '<div style="font-family:JetBrains Mono,monospace;font-size:.68rem;color:var(--muted);margin-bottom:10px">Mission ' + esc(mission.id) + ' · stage: ' + esc(mission.stage || '—') + ' · ' + mission.audit.length + ' event(s)</div>'
+    + rows;
 }
 
 function pullSLAReport(){