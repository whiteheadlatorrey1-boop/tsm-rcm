--- healthcare/executive-portal.html	2026-08-15 02:28:58.509926362 +0000
+++ tmp/executive-portal-test.html	2026-08-15 10:45:47.741661049 +0000
@@ -3075,7 +3075,12 @@
         setKPI('#kpi-no-action-rev',   noAction);
         setKPI('#kpi-action-rev',      withAction);
         setKPI('#kpi-recovery-time',   recovery);
-        setKPI('#kpi-confidence',      confidence);
+        // NOTE: '#kpi-confidence' intentionally NOT wired here — that id is
+        // reused by the unrelated "Appeal Win Rate" KPI tile, so writing the
+        // Strategist's AI Confidence Score to it was overwriting Appeal Win
+        // Rate with a blank dash whenever no confidence value was available.
+        // The two are different metrics; a dedicated confidence display
+        // should get its own element id/data-kpi before this is re-wired.
 
         // Also surface in the Decision Center audit row if present
         setKPI('#exec-rev-at-risk',    noAction);
