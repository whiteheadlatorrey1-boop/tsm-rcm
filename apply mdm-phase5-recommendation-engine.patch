diff --git a/html/mdm-suite/mdm-core.js b/html/mdm-suite/mdm-core.js
index a1a7e74..d1ee356 100644
--- a/html/mdm-suite/mdm-core.js
+++ b/html/mdm-suite/mdm-core.js
@@ -156,4 +156,79 @@ function scoreDataset(records, domain) {
   return { domain, avgScore, recordCount: records.length, scores };
 }
 
-module.exports = { findDuplicates, scoreDataset, scoreRecord, recordSimilarity, similarity };
\ No newline at end of file
+// ---------------------------------------------------------------------------
+// Phase 5 — Recommendation engine. Deterministic, no LLM call: every
+// recommendation traces to a real duplicate match plus the quality score of
+// both candidate records, and (when a mergeLog is supplied) to real past
+// decisions for the same domain/matchReason pair. This is the "explain why"
+// data the strategist UI and /api/mdm/recommendations route consume.
+// ---------------------------------------------------------------------------
+
+function recIdFor(domain, match) {
+  return `REC-${domain}-${match.recordA.id}-${match.recordB.id}`;
+}
+
+// Counts prior APPROVED/REJECTED decisions in the same domain that used the
+// same matchReason (identifier_exact vs fuzzy_name) — this is the real
+// "resolved N times before" signal, computed from the actual decision log
+// rather than invented.
+function historicalPatternFor(domain, matchReason, mergeLog) {
+  const log = Array.isArray(mergeLog) ? mergeLog : [];
+  const priorApproved = log.filter(h =>
+    h.domain === domain &&
+    h.decision === 'APPROVED' &&
+    (h.matchReason ? h.matchReason === matchReason : true)
+  );
+  const priorRejected = log.filter(h =>
+    h.domain === domain &&
+    h.decision === 'REJECTED' &&
+    (h.matchReason ? h.matchReason === matchReason : true)
+  );
+  return {
+    approvedCount: priorApproved.length,
+    rejectedCount: priorRejected.length,
+    lastDecisionAt: priorApproved.length ? priorApproved[priorApproved.length - 1].ts : null
+  };
+}
+
+// Risk = how much is exposed if this duplicate is left unresolved. Combines
+// how bad the worse-scoring record's quality is with how confident the match
+// itself is. 0-100, higher = more urgent.
+function riskFor(match, records) {
+  const a = records.find(r => r.id === match.recordA.id) || {};
+  const b = records.find(r => r.id === match.recordB.id) || {};
+  const worstQuality = Math.min(
+    a.quality != null ? a.quality : 100,
+    b.quality != null ? b.quality : 100
+  );
+  const qualityRisk = 100 - worstQuality;
+  const matchRisk = match.matchScore;
+  return Math.round(qualityRisk * 0.4 + matchRisk * 0.6);
+}
+
+function buildRecommendations(records, duplicateMatches, domain, mergeLog) {
+  return duplicateMatches.map(m => {
+    const pattern = historicalPatternFor(domain, m.matchReason, mergeLog);
+    const risk = riskFor(m, records);
+    const requiresApproval = !(m.matchReason === 'identifier_exact' && m.matchScore >= 95);
+    return {
+      id: recIdFor(domain, m),
+      domain,
+      issue: `Duplicate ${domain} identities: ${m.recordA.id} \u2194 ${m.recordB.id}`,
+      risk,
+      action: 'MERGE_RECORDS',
+      confidence: m.matchScore,
+      requiresApproval,
+      survivorId: m.recordA.id,
+      mergedId: m.recordB.id,
+      matchReason: m.matchReason,
+      matchField: m.matchField || null,
+      historicalPattern: pattern
+    };
+  }).sort((x, y) => y.risk - x.risk);
+}
+
+module.exports = {
+  findDuplicates, scoreDataset, scoreRecord, recordSimilarity, similarity,
+  buildRecommendations, recIdFor, historicalPatternFor, riskFor
+};
\ No newline at end of file
diff --git a/html/war-rooms/mdm/mdm-executive-portal.html b/html/war-rooms/mdm/mdm-executive-portal.html
index 66ca2d4..2eafa6d 100644
--- a/html/war-rooms/mdm/mdm-executive-portal.html
+++ b/html/war-rooms/mdm/mdm-executive-portal.html
@@ -76,7 +76,7 @@ body{background:var(--bg);color:var(--text);font-family:'Rajdhani',sans-serif;mi
 function tick(){document.getElementById('clock').textContent=new Date().toLocaleTimeString();}
 setInterval(tick,1000);tick();
 
-const CFG = {"domain": "MDM", "kpiPath": "kpis", "kpis": [{"key": "total_records", "label": "TOTAL RECORDS", "fmt": "num", "cls": "cyan"}, {"key": "duplicate_count", "label": "DUPLICATES", "fmt": "num", "cls": "red"}, {"key": "quality_score", "label": "QUALITY SCORE", "fmt": "pct", "cls": "green"}, {"key": "pending_approvals", "label": "PENDING MERGES", "fmt": "num", "cls": "amber"}, {"key": "anomalies", "label": "ANOMALIES", "fmt": "num", "cls": "red"}, {"key": "stewards_active", "label": "ACTIVE STEWARDS", "fmt": "num", "cls": "cyan"}], "lists": [{"path": "duplicates", "label": "DUPLICATE CLUSTERS"}], "aiPaths": ["ai_analysis"], "title": "Master Data Management", "isExec": true};
+const CFG = {"domain": "MDM", "kpiPath": "kpis", "kpis": [{"key": "quality_score", "label": "ENTERPRISE DATA TRUST", "fmt": "pct", "cls": "green"}, {"key": "total_records", "label": "TOTAL RECORDS", "fmt": "num", "cls": "cyan"}, {"key": "duplicate_count", "label": "DUPLICATES", "fmt": "num", "cls": "red"}, {"key": "pending_approvals", "label": "PENDING MERGES", "fmt": "num", "cls": "amber"}, {"key": "anomalies", "label": "ANOMALIES", "fmt": "num", "cls": "red"}, {"key": "ai_recommendations", "label": "AI RECOMMENDATIONS", "fmt": "num", "cls": "amber"}, {"key": "automations_executed", "label": "AUTOMATIONS EXECUTED", "fmt": "num", "cls": "green"}, {"key": "stewards_active", "label": "ACTIVE STEWARDS", "fmt": "num", "cls": "cyan"}], "lists": [{"path": "duplicates", "label": "DUPLICATE CLUSTERS"}], "aiPaths": ["ai_analysis"], "title": "Master Data Management", "isExec": true};
 const RELAY_KEY_FALLBACK = 'TSM_' + CFG.domain + '_RELAY';
 
 function showToast(msg){
diff --git a/html/war-rooms/mdm/mdm-strategist.html b/html/war-rooms/mdm/mdm-strategist.html
index 90e6abc..cbe7e83 100644
--- a/html/war-rooms/mdm/mdm-strategist.html
+++ b/html/war-rooms/mdm/mdm-strategist.html
@@ -66,6 +66,15 @@ body{background:var(--bg);color:var(--text);font-family:'Courier New',monospace;
 .history-row{display:flex;gap:10px;padding:6px 10px;border-bottom:1px solid var(--border);font-size:9px;color:var(--text-dim);flex-wrap:wrap}
 .history-row .decision-approved{color:var(--green)} .history-row .decision-rejected{color:var(--red)}
 .history-row .src{color:var(--gold)}
+
+.rec-card{background:var(--bg3);border:1px solid var(--border);border-left:2px solid var(--amber);margin-bottom:10px;padding:12px}
+.rec-card.risk-high{border-left-color:var(--red)}
+.rec-card.risk-low{border-left-color:var(--green)}
+.rec-hdr{display:flex;justify-content:space-between;align-items:center;margin-bottom:6px}
+.rec-issue{color:var(--text);font-size:10px}
+.rec-meta{display:flex;gap:10px;font-size:8px;color:var(--text-dim);margin-bottom:8px;flex-wrap:wrap}
+.rec-meta b{color:var(--gold)}
+.rec-pattern{background:var(--bg2);border:1px solid var(--border);padding:6px 8px;font-size:8px;color:var(--text-dim);margin-bottom:8px}
 </style></head><body>
 <div class="nav">
   <div class="nav-brand">TSM SHELL // MDM STRATEGIST</div>
@@ -88,6 +97,14 @@ body{background:var(--bg);color:var(--text);font-family:'Courier New',monospace;
     <div class="hero-title">MASTER DATA <b style="color:var(--green)">STRATEGIST</b></div>
     <div class="hero-sub">Judgment-tier review for duplicate matches the engine can't resolve on its own.</div>
   </div>
+  <div class="section">
+    <div class="section-head"><span>AI RECOMMENDATIONS &middot; LIVE FROM DECISION ENGINE</span><span id="recMeta">loading&hellip;</span></div>
+    <div class="section-body" id="recBody"><div style="color:var(--text-dim);padding:16px;text-align:center">Loading recommendations&hellip;</div></div>
+  </div>
+  <div class="section">
+    <div class="section-head"><span>HISTORICAL DECISIONS &middot; ALL SESSIONS</span><span id="histMeta">loading&hellip;</span></div>
+    <div class="section-body" id="histBody"><div style="color:var(--text-dim);padding:16px;text-align:center">Loading history&hellip;</div></div>
+  </div>
   <div id="content"><div class="empty">Loading relay from War Room&hellip;</div></div>
 </div>
 
@@ -247,6 +264,107 @@ body{background:var(--bg);color:var(--text);font-family:'Courier New',monospace;
   window.addEventListener('storage', e=>{
     if(e.key === 'TSM_MDM_RELAY') loadRelay();
   });
+
+  /* ---------- Phase 5: AI Recommendation Queue ----------
+     Independent of the relay — pulled straight from GET /api/mdm/recommendations,
+     the same deterministic engine the war room's duplicate detection runs on.
+     Requires no manual "relay to strategist" click to show up. */
+
+  function riskClass(risk){ return risk>=70?'risk-high':risk<40?'risk-low':''; }
+
+  function patternText(p, domain){
+    if(!p || (!p.approvedCount && !p.rejectedCount)) return `No prior ${domain} decisions of this type on record yet \u2014 this would be the first.`;
+    const bits = [];
+    if(p.approvedCount) bits.push(`approved ${p.approvedCount} time${p.approvedCount===1?'':'s'}`);
+    if(p.rejectedCount) bits.push(`rejected ${p.rejectedCount} time${p.rejectedCount===1?'':'s'}`);
+    return `Similar ${domain} matches were previously ${bits.join(', ')}` + (p.lastDecisionAt?` \u2014 most recently ${new Date(p.lastDecisionAt).toLocaleDateString()}`:'') + '.';
+  }
+
+  function recCard(r){
+    return `<div class="rec-card ${riskClass(r.risk)}" data-id="${r.id}">
+      <div class="rec-hdr">
+        <span class="rec-issue">${r.issue}</span>
+        <span class="badge ${r.requiresApproval?'badge-amber':'badge-green'}">${r.requiresApproval?'NEEDS APPROVAL':'HIGH CONFIDENCE'}</span>
+      </div>
+      <div class="rec-meta">
+        <span>Risk: <b>${r.risk}</b></span>
+        <span>Confidence: <b>${r.confidence}%</b></span>
+        <span>Action: <b>${r.action}</b></span>
+        <span>Match basis: <b>${r.matchReason==='identifier_exact'?('shared '+r.matchField):'fuzzy name'}</b></span>
+      </div>
+      <div class="rec-pattern">WHY: ${patternText(r.historicalPattern, r.domain)}</div>
+      <div class="btn-row">
+        <button class="btn btn-green" data-rec-action="approve" data-id="${r.id}">APPROVE &middot; KEEP ${r.survivorId}</button>
+        <button class="btn btn-ghost" data-rec-action="reject" data-id="${r.id}">REJECT</button>
+      </div>
+    </div>`;
+  }
+
+  async function loadRecommendations(){
+    try{
+      const res = await fetch('/api/mdm/recommendations');
+      const d = await res.json();
+      if(!d.ok) throw new Error(d.error || 'Failed to load recommendations');
+      const recs = d.recommendations || [];
+      document.getElementById('recMeta').textContent = `${recs.length} open`;
+      document.getElementById('recBody').innerHTML = recs.length
+        ? recs.map(recCard).join('')
+        : '<div style="color:var(--text-dim);padding:16px;text-align:center">No open recommendations \u2014 engine sees nothing worth flagging right now.</div>';
+    } catch(err){
+      document.getElementById('recMeta').textContent = 'error';
+      document.getElementById('recBody').innerHTML = `<div style="color:var(--red);padding:16px;text-align:center">Failed to load: ${err.message}</div>`;
+    }
+  }
+
+  document.getElementById('recBody').addEventListener('click', async e=>{
+    const btn = e.target.closest('button[data-rec-action]');
+    if(!btn) return;
+    const { recAction, id } = btn.dataset;
+    const actor = window.prompt('Strategist name for this decision:', 'R. Whitfield');
+    if(actor===null) return;
+    btn.closest('.rec-card').style.opacity = '0.5';
+    try{
+      const res = await fetch(`/api/mdm/recommend/${encodeURIComponent(id)}/${btn.dataset.recAction}`, {
+        method:'POST', headers: authHeaders(), body: JSON.stringify({ actor })
+      });
+      const d = await res.json();
+      if(!d.ok) throw new Error(d.error || 'Decision failed');
+      await loadRecommendations();
+      await loadHistory();
+    } catch(err){
+      alert(`Decision failed: ${err.message}`);
+      btn.closest('.rec-card').style.opacity = '1';
+    }
+  });
+
+  /* ---------- Persisted cross-session decision history ----------
+     Reads GET /api/mdm/merge-history, which is backed by the server-side
+     MDM_MERGE_LOG — real audit trail, not the page's in-memory sessionLog. */
+
+  async function loadHistory(){
+    try{
+      const res = await fetch('/api/mdm/merge-history');
+      const d = await res.json();
+      if(!d.ok) throw new Error(d.error || 'Failed to load history');
+      const log = d.log || [];
+      document.getElementById('histMeta').textContent = `${log.length} decision${log.length===1?'':'s'}`;
+      document.getElementById('histBody').innerHTML = log.length
+        ? log.map(h=>`<div class="history-row">
+            <span>${new Date(h.ts).toLocaleString()}</span>
+            <span class="decision-${h.decision.toLowerCase()}">${h.decision}</span>
+            <span>${h.domain}: kept ${h.survivorId} over ${h.mergedId}</span>
+            <span class="src">${h.source || 'war-room'}${h.matchScore!=null?(' \u00b7 '+h.matchScore+'%'):''}</span>
+            <span>&mdash; ${h.actor}</span>
+          </div>`).join('')
+        : '<div style="color:var(--text-dim);padding:12px;text-align:center">No decisions on record yet.</div>';
+    } catch(err){
+      document.getElementById('histMeta').textContent = 'error';
+      document.getElementById('histBody').innerHTML = `<div style="color:var(--red);padding:12px;text-align:center">Failed to load: ${err.message}</div>`;
+    }
+  }
+
+  loadRecommendations();
+  loadHistory();
 })();
 </script>
   <script src="/html/core/tsm-runtime.js"></script>
diff --git a/html/war-rooms/mdm/mdm-war-room.html b/html/war-rooms/mdm/mdm-war-room.html
index d36cfd6..8456a70 100644
--- a/html/war-rooms/mdm/mdm-war-room.html
+++ b/html/war-rooms/mdm/mdm-war-room.html
@@ -25,7 +25,7 @@ body{background:var(--bg);color:var(--text);font-family:'Courier New',monospace;
 .domain-item{display:flex;justify-content:space-between;align-items:center;padding:5px 8px;background:var(--bg3);border:1px solid var(--border);font-size:9px}
 .domain-name{color:var(--text)}  .domain-count{color:var(--green);font-weight:700}
 .main{overflow-y:auto;background:var(--bg);padding:18px 22px}
-.kpi-grid{display:grid;grid-template-columns:repeat(6,1fr);gap:10px;margin-bottom:20px}
+.kpi-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(110px,1fr));gap:10px;margin-bottom:20px}
 .kpi-card{background:var(--card);border:1px solid var(--border);padding:12px 10px}
 .kpi-label{font-size:8px;letter-spacing:1px;color:var(--text-dim);margin-bottom:6px;text-transform:uppercase}
 .kpi-value{font-size:18px;font-weight:700;color:var(--green)}
@@ -107,7 +107,9 @@ td{padding:7px 8px;border-bottom:1px solid var(--border);color:var(--text)}
     { id:'quality_score',    label:'Data Quality',     unit:'pct'   },
     { id:'pending_approvals',label:'Pending Merges',   unit:'count' },
     { id:'anomalies',        label:'AI Anomalies',     unit:'count' },
-    { id:'stewards_active',  label:'Active Stewards',  unit:'count' }
+    { id:'stewards_active',  label:'Active Stewards',  unit:'count' },
+    { id:'ai_recommendations', label:'AI Recommendations', unit:'count' },
+    { id:'automations_executed', label:'Automations Executed', unit:'count' }
   ];
   let records=[], duplicates=[], mergeLog=[];
   function tick(){ document.getElementById('navClock').textContent=new Date().toLocaleTimeString(); }
@@ -124,7 +126,14 @@ td{padding:7px 8px;border-bottom:1px solid var(--border);color:var(--text)}
     const quals=records.map(r=>r.quality||0);
     const avgQ=quals.length?Math.round(quals.reduce((a,b)=>a+b,0)/quals.length):0;
     const anomalies=records.filter(r=>r.status==='INCOMPLETE').length;
-    return { total_records:records.length, duplicate_count:dups, quality_score:avgQ, pending_approvals:duplicates.length, anomalies, stewards_active:new Set(records.map(r=>r.steward)).size };
+    const automationsExecuted=mergeLog.filter(h=>h.decision==='APPROVED').length;
+    return {
+      total_records:records.length, duplicate_count:dups, quality_score:avgQ,
+      pending_approvals:duplicates.length, anomalies,
+      stewards_active:new Set(records.map(r=>r.steward)).size,
+      ai_recommendations:duplicates.length,
+      automations_executed:automationsExecuted
+    };
   }
   function renderAll(){
     const k=computeKpis(); renderKpis(k); renderDomains(); renderRecords(); renderMerges(); renderHistory(); renderStatus(k);
diff --git a/server.js b/server.js
index 392440e..948d000 100644
--- a/server.js
+++ b/server.js
@@ -2016,7 +2016,10 @@ app.post('/api/integration/query', async (req, res) => {
 
 
 // ── PHASE 6: MASTER DATA MANAGEMENT (MDM) ──────────────────────────────────────
-const { findDuplicates: mdmFindDuplicates, scoreDataset: mdmScoreDataset } = require('./html/mdm-suite/mdm-core.js');
+const {
+  findDuplicates: mdmFindDuplicates, scoreDataset: mdmScoreDataset,
+  buildRecommendations: mdmBuildRecommendations
+} = require('./html/mdm-suite/mdm-core.js');
 const MDM_SEED_DATA = require('./html/mdm-suite/mdm-seed-data.json');
 // Deep-cloned at load time, before any merge can mutate MDM_SEED_DATA, so a real
 // reset is possible (restores retired records and clears the decision log).
@@ -2108,16 +2111,20 @@ function requireApiKey(req, res, next) {
   next();
 }
 
-app.post('/api/mdm/merge', requireApiKey, (req, res) => {
-  const { domain, survivorId, mergedId, actor, decision } = req.body || {};
+// Shared decision-execution path — used by both the manual /merge endpoint
+// (war room / strategist side-by-side review) and the /recommend/:id
+// approve|reject endpoints (Phase 5 AI recommendation queue). One code path
+// means one place that actually retires a losing record, so the two entry
+// points can never drift into different behavior.
+function executeMdmDecision({ domain, survivorId, mergedId, actor, decision, extra }) {
   if (!domain || !survivorId || !mergedId) {
-    return res.status(400).json({ ok: false, error: 'domain, survivorId, mergedId required' });
+    return { error: { status: 400, body: { ok: false, error: 'domain, survivorId, mergedId required' } } };
   }
   const raw = MDM_SEED_DATA[domain];
-  if (!raw) return res.status(404).json({ ok: false, error: 'Unknown domain' });
+  if (!raw) return { error: { status: 404, body: { ok: false, error: 'Unknown domain' } } };
   const survivor = raw.find(r => r.id === survivorId);
   const merged = raw.find(r => r.id === mergedId);
-  if (!survivor || !merged) return res.status(404).json({ ok: false, error: 'Record not found in domain' });
+  if (!survivor || !merged) return { error: { status: 404, body: { ok: false, error: 'Record not found in domain' } } };
 
   const entry = {
     id: `MRG-${Date.now()}-${Math.floor(Math.random()*1000)}`,
@@ -2125,7 +2132,8 @@ app.post('/api/mdm/merge', requireApiKey, (req, res) => {
     survivorName: survivor.name, mergedName: merged.name,
     decision: decision === 'REJECTED' ? 'REJECTED' : 'APPROVED',
     actor: actor || 'Unassigned',
-    ts: new Date().toISOString()
+    ts: new Date().toISOString(),
+    ...(extra || {})
   };
   MDM_MERGE_LOG.push(entry);
 
@@ -2135,13 +2143,77 @@ app.post('/api/mdm/merge', requireApiKey, (req, res) => {
     MDM_SEED_DATA[domain] = raw.filter(r => r.id !== mergedId);
   }
 
-  res.json({ ok: true, entry });
+  return { entry };
+}
+
+app.post('/api/mdm/merge', requireApiKey, (req, res) => {
+  const { domain, survivorId, mergedId, actor, decision, matchScore, matchReason, matchField, source } = req.body || {};
+  const result = executeMdmDecision({
+    domain, survivorId, mergedId, actor, decision,
+    extra: { matchScore, matchReason, matchField, source: source || 'war-room' }
+  });
+  if (result.error) return res.status(result.error.status).json(result.error.body);
+  res.json({ ok: true, entry: result.entry });
 });
 
 app.get('/api/mdm/merge-history', (req, res) => {
   res.json({ ok: true, log: MDM_MERGE_LOG.slice(-200).reverse() });
 });
 
+// ── Phase 5: AI Recommendation Queue ────────────────────────────────────────
+// Deterministic recommendations derived from the same duplicate-detection
+// engine the war room already uses, plus real historical-pattern data pulled
+// from MDM_MERGE_LOG. No fabricated stewardship state, no separate "decision
+// engine" data source to drift out of sync with /api/mdm/detail.
+app.get('/api/mdm/recommendations', (req, res) => {
+  const domains = Object.keys(MDM_SEED_DATA);
+  let recommendations = [];
+  domains.forEach(d => {
+    const raw = MDM_SEED_DATA[d];
+    const scored = mdmScoreDataset(raw, d);
+    const dupes = mdmFindDuplicates(raw, d);
+    const recordsWithQuality = raw.map(r => ({
+      ...r,
+      quality: (scored.scores.find(s => s.recordId === r.id) || {}).overall ?? 0
+    }));
+    recommendations = recommendations.concat(
+      mdmBuildRecommendations(recordsWithQuality, dupes, d, MDM_MERGE_LOG)
+    );
+  });
+  recommendations.sort((a, b) => b.risk - a.risk);
+  res.json({ ok: true, recommendations });
+});
+
+function findRecommendationById(id) {
+  const domains = Object.keys(MDM_SEED_DATA);
+  for (const d of domains) {
+    const raw = MDM_SEED_DATA[d];
+    const dupes = mdmFindDuplicates(raw, d);
+    const recs = mdmBuildRecommendations(raw, dupes, d, MDM_MERGE_LOG);
+    const hit = recs.find(r => r.id === id);
+    if (hit) return hit;
+  }
+  return null;
+}
+
+function recommendDecision(decision) {
+  return (req, res) => {
+    const rec = findRecommendationById(req.params.id);
+    if (!rec) return res.status(404).json({ ok: false, error: 'Unknown or already-resolved recommendation id' });
+    const { actor } = req.body || {};
+    const result = executeMdmDecision({
+      domain: rec.domain, survivorId: rec.survivorId, mergedId: rec.mergedId,
+      actor, decision,
+      extra: { matchScore: rec.confidence, matchReason: rec.matchReason, matchField: rec.matchField, source: 'ai-recommendation', recommendationId: rec.id }
+    });
+    if (result.error) return res.status(result.error.status).json(result.error.body);
+    res.json({ ok: true, entry: result.entry, recommendation: rec });
+  };
+}
+
+app.post('/api/mdm/recommend/:id/approve', requireApiKey, recommendDecision('APPROVED'));
+app.post('/api/mdm/recommend/:id/reject', requireApiKey, recommendDecision('REJECTED'));
+
 // Real reset: restores every domain to its original seeded state (undoes any
 // approved merges) and clears the decision log. Previously "RESET DATA" just
 // re-fetched current state with no way to actually undo anything.